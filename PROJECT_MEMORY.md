# PROJECT_MEMORY.md

**This is the permanent memory of Noivos.** It is never overwritten — only appended to. Every future conversation, document, and engineering decision must be reconciled against this file before new work begins. If something here conflicts with a new request, stop and ask the founder before proceeding.

Last updated: 2026-08-02 (Phase 1 — PRD open questions resolved)

---

## 1. Company Vision

Noivos is building the operating system for a couple's financial life together — starting with engaged couples planning a wedding, and growing with them into marriage, home ownership, family, and long-term wealth building.

We believe the strongest financial products for couples won't look like banks. They'll look like the best consumer software of this era — Apple, Notion, Linear, Duolingo — applied to the most emotionally loaded, most avoided conversation two people in love will have: money.

## 2. Mission

Help couples make smarter financial decisions **together** by replacing financial confusion, stress, and secrecy with clarity, conversation, and confidence — without judgment.

## 3. Core Product Philosophy

- **Relationship first, finance second.** The relationship is the product; money is the tool.
- Budgeting, expense tracking, and wedding planning are **features**, not the product.
- Shared financial awareness, not financial surveillance.
- The AI is a supportive teammate — never a banker, never an accountant, never a source of shame.
- No financial comparison between users, ever. Community is for accountability, not competition.
- Every feature must be evaluated against: *does this help two people make a better financial decision together?* If not, question whether it belongs.

## 4. Product Decisions (Approved)

| Area | Decision | Status |
|---|---|---|
| Product category | Financial companion for couples, not a budgeting app clone (not EveryDollar/YNAB/Monarch clone) | Approved (founder brief) |
| Core entity | Two individuals connect into a shared workspace ("Partnership"/"Relationship") that layers on top of, not replaces, personal finances | Approved |
| Wedding Mode | Dedicated mode with a "Graduate" action that archives wedding data as a keepsake and transitions the couple into married financial planning | Approved |
| Free tier | Expense tracking, budgets, goals, basic insights, relationship workspace — no Plaid, no AI | Approved |
| Premium tier | $100/year (~15% cheaper than paying monthly) — gates Plaid, AI Purchase Advisor, AI Financial Coach, advanced insights, OCR/camera/receipt scanning | Approved |
| Community | No income, balance, net worth, spending, or debt sharing under any circumstance. Optional sharing limited to goals, progress %, milestones, achievements, encouragement, challenges | Approved |
| Relationship lifecycle | Users can disconnect and later reconnect with a new partner; former shared data must archive without leaking to a new partnership | Approved (mechanics TBD in Database Architecture) |
| Money movement scope | Noivos is a **read-only** aggregation and planning layer only. No custody, no transfers between partners, no Noivos-branded account/card, at least through V1 | Approved 2026-08-02 |
| Premium billing unit | Billed **per Partnership** (couple), not per individual — one payer unlocks Premium for the shared workspace and both partners' personal spaces | Approved 2026-08-02 |
| Launch geography | **US-only, USD, English** at launch | Approved 2026-08-02 |
| AI Financial Coach posture | Strictly **educational, scenario-based** guidance only — never names specific lenders/products, never issues directives ("you should refinance"). Legal review required before public launch | Approved 2026-08-02 |
| Shared-workspace entity name | Called **"Partnership"** internally in the data model and all docs; user-facing copy may stay warmer ("Your Relationship," "You & [Name]") | Approved 2026-08-02 |
| Premium free trial | **14-day free trial**, gated behind partner-invite acceptance — both partners must be in before the trial starts | Approved 2026-08-02 |
| Partnership disconnect visibility | The non-initiating partner retains **frozen, read-only** access to shared history as it stood at disconnect; nothing new syncs to either side going forward | Approved 2026-08-02 |

## 5. Brand Decisions

### 5.0 Reversal (2026-08-02)

The founder directly confirmed the same day both rounds happened: **the Phase 1 moodboard (§5.2) is the default brand system, not the text Brand Statement's palette/typography (§5.1).** §5.1's visual specifics (Electric Purple/Sora color and type system) are superseded by the moodboard wherever the two disagree. §5.1's *non-visual* content — personality, keywords, tone of voice, AI personality, design principles, motion, photography, website direction, brand promise, north star — does not conflict with the moodboard and remains in force. Both rounds are kept in full below per the "never delete history" rule; `docs/03 UX/Brand Guidelines.md` v2.0 reflects this reversal.

### 5.1 Text Brand Statement (2026-08-02) — superseded on palette/type only, non-visual content still in force

The founder delivered a full, formal Brand Statement on 2026-08-02, drafted into `docs/03 UX/Brand Guidelines.md` v1.0. Its color system and typography are **superseded by the Phase 1 moodboard (§5.2)** per §5.0 above. Preserved here in full so no content is lost:

- **Brand statement:** a lifestyle brand that happens to help couples make smarter financial decisions together — closer to Sour Strips/Notion/Apple/Poppi/Liquid Death than a banking app. (Still in force — and notably, Sour Strips/Liquid Death are themselves bold, high-saturation brands, which is more consistent with the moodboard's neon energy than it first appeared.)
- **Personality:** Confident, Optimistic, Human, Playful (without childish), Premium, Relatable. (Still in force.)
- **Color system — superseded:** Primary Electric Purple `#6B4EFF`; Secondary Hot Coral `#FF6B6B`; Accent Acid Lime `#C7FF3D`; Success Mint `#45E6B1`; Warning Mango `#FFB547`; Background Warm Ivory `#FBF9F4`; Dark mode Midnight `#161616`; Text Charcoal `#1F1F1F`. No longer the default — see §5.2 for the palette now in force.
- **Typography — superseded:** Sora headlines / Inter body. No longer the default for headlines — see §5.2 (Bebas Neue Bold). Inter for body text and tabular numbers for all numeric display are still in force (both rounds agreed on this).
- **Iconography:** Lucide or Phosphor for secondary/utility icons — still in force as a candidate list (choice still open). Primary nav icons now follow the moodboard's bespoke colorful set instead.
- **Illustration:** hand-drawn doodles/stickers/confetti/organic shapes — reinterpreted as the *secondary/accent layer* on top of the moodboard's bolder primary illustration style, not a replacement for it (see §5.2 and Brand Guidelines §8).
- **Photography:** only authentic lifestyle moments; never corporate/handshake/suit imagery. Still in force — the moodboard didn't address photography.
- **Logo:** still the moodboard's gradient "N" mark — no redesign needed, since the moodboard is now the default rather than a superseded predecessor. Production (final vector, monochrome, app-icon crop) is still outstanding design work.
- **Design principles:** celebrate progress; real life first; personality over polish; build trust through simplicity — still in force. "Calm before color" is **reinterpreted** (2026-08-02) as restraint in layout/information density rather than in palette saturation, since the now-default moodboard palette is vivid throughout, not reserved for rare accents.
- **Brand promise:** "We help couples make smarter financial decisions — together. Not by telling them what to do. By giving them the clarity and confidence to decide together." Still in force.
- **North star:** Build a brand people wear on a hoodie, not just an app they download. Still in force.

### 5.2 Phase 1 Brand Moodboard (2026-08-02) — **current default brand system**

A first-pass brand moodboard was supplied by the founder on 2026-08-02 (stored at `docs/assets/brand/brand-moodboard-v1.png`). Originally logged as input to Phase 2 rather than a locked system, the founder has since confirmed **this is the brand to default to** — its palette, typography, logo, and illustration energy are the approved visual identity, superseding §5.1's palette/type where they disagree.

- **Tagline:** "Better Money. Together."
- **Logo concept:** Bold gradient "N" mark — a nudge forward, partnership, momentum, always moving forward together.
- **Color palette (working names):**
  - Sour Lime `#C6FF00` — Primary
  - Sour Punch `#FF2D8E` — Secondary
  - Electric Blue `#0066FF` — Accent
  - Citrus `#FFE600` — Highlight
  - Grape `#8A2BE2` — Info
  - Sour Cloud `#F5F5F7` — Surface (light)
  - Licorice `#0D0D0F` — Dark background
- **Typography:** Headings in Bebas Neue Bold (condensed, high-energy display face); body in Inter.
- **Iconography:** Home, Goals, Insights, Activity, AI Coach, Plan, Budget, Savings, Milestone, More.
- **Illustration style:** Bold, high-contrast, graffiti/scrapbook-energy illustrations (e.g., "Together," "Build a Life," "Reach Goals," "Celebrate Wins"), not flat corporate iconography.
- **UI elements:** Pill-shaped primary/secondary buttons, dark-mode-first surfaces, celebratory milestone cards.
- **Brand vibe (as given):** Bold & Energetic, Warm & Human, Fun & Approachable, Trustworthy & Secure, Forward Momentum.

**Open tension to resolve in Phase 2:** this moodboard skews closer to a Gen-Z lifestyle/candy-brand aesthetic (very high-saturation neon-on-black) than the "Apple / Notion / Linear" reference points named in the founder brief, which read calmer and more restrained. Both can be reconciled (energetic accent colors on a calm neutral base), but Phase 2 needs to explicitly decide how "loud" the day-to-day app UI is versus how loud the marketing site / brand moments are. Flagged, not decided.

**Resolved 2026-08-02:** the founder confirmed this moodboard — loud palette and all — is the default brand system, not a calmer compromise. The "Calm before color" principle from §5.1 is reinterpreted accordingly (restraint in layout/density, not in saturation) rather than treated as a mandate to mute the palette.

**Locked 2026-08-02, ahead of Phase 4 (Design System):** icon library is **Lucide** for secondary/utility icons (primary nav stays bespoke); Success/Warning colors **reuse Sour Lime and Citrus** rather than adding new hexes.

**Confirmed 2026-08-02:** the moodboard is a reference image only, not a live Canva design — Phase 2 will build the real brand system from scratch using it as creative direction, not by pulling forward an existing Canva asset.

## 6. Technical Decisions

| Layer | Choice | Notes |
|---|---|---|
| Mobile/Web frontend | React Native + Expo | Shared codebase target for iOS/Android; web app architecture TBD in Frontend Architecture doc |
| Backend | Vercel (Next.js Route Handlers) + Neon Postgres | **Superseded 2026-08-02 — was Supabase.** See §6.4 below for the full pivot and reasoning. |
| Database | PostgreSQL via **Neon** | ~~Via Supabase~~ — superseded 2026-08-02. RLS-based privacy design (Database Architecture §10) is unaffected, since RLS is a Postgres feature, not a Supabase one. |
| Auth | **Clerk** | ~~Supabase Auth~~ — superseded 2026-08-02. Email, Apple, Google still required (PRD §12.1); Clerk supports all three and works across both Next.js (web) and Expo (mobile). |
| Payments | Stripe (web) at launch; Apple IAP + Google Play Billing to be added when native mobile ships | **Approved 2026-08-02: launch web-only first on Stripe. When mobile apps ship, mobile purchases move to Apple IAP / Google Play Billing (not Stripe on-device) to stay compliant with store policy — a shared entitlements service reconciles whichever route a user paid through. See §9 for remaining engineering risk.** |
| Banking data | Plaid | Checking, savings, credit, loans; manual accounts as fallback |
| AI | OpenAI Responses API | Text, vision (receipt/price-tag scanning), voice input |
| Storage | **Vercel Blob** | ~~Supabase Storage~~ — superseded 2026-08-02. Receipts, attachments, avatars. Chosen since the backend is already Vercel-hosted — one less vendor to integrate. |
| Push notifications | Firebase Cloud Messaging + Apple Push Notification service | |
| Analytics | PostHog | |
| Monitoring | Sentry | |

No infrastructure, hosting, or CI/CD decisions have been made yet — deferred to the Infrastructure document (Phase 10).

### 6.4 Supabase → Neon/Clerk/Vercel Blob Pivot (2026-08-02)

Supabase was unworkable in the founder's environment ("unable to do Supabase"). Rather than block, the stack was re-pointed to keep moving:

- **Database:** Neon replaces Supabase for Postgres hosting. This is a low-disruption swap for everything already documented in `docs/04 Database/Database Architecture.md` — RLS (§10), the schema (§3–§9), and the one-active-Partnership constraint are all plain Postgres features, not Supabase-specific, so none of that redesigns. **Neon also has its own database-branching feature**, directly replacing the "Supabase branch per environment" plan in Backend Architecture §10 — same environment model (dev/staging/prod branches), different provider.
- **Auth:** Clerk replaces Supabase Auth. Still needs to support Email, Apple, Google (PRD §12.1) — Clerk does, and works across both the Next.js web app and the Expo mobile app, which Supabase Auth would have too, so this is a like-for-like swap, not a scope change.
- **Storage:** Vercel Blob replaces Supabase Storage for receipts/attachments/avatars — picked because the backend is already Vercel-hosted (Backend Architecture §1), one less vendor rather than a deliberate feature comparison.
- **What does NOT change:** the Vercel API layer, the Plaid/Stripe/AI architecture, the RLS-based privacy model, and the schema itself — all of `docs/07 Backend/Backend Architecture.md` and `docs/04 Database/Database Architecture.md` remain valid except for their Supabase-specific mechanics (Supabase Vault for token encryption, Supabase branching, Supabase Auth/Storage references), which are superseded by this entry rather than rewritten line-by-line in those documents.
- **Plaid token encryption (Database Architecture §14):** Supabase Vault is no longer available. **Not yet decided** — needs a replacement (e.g., `pgcrypto` with an application-managed key, or a dedicated secrets manager) before Plaid integration is built for real. Flagged, not resolved.
- These were fast calls made under the founder's "move as fast as possible" direction, not put to a fresh round of questions — flagged here precisely so they're easy to revisit if wrong, per the same pattern used for every other fast-tracked decision this session.

**Real Clerk key received and tested 2026-08-03:** founder provided a real publishable + secret key pair. Testing (both in `apps/mobile`'s web export and in the new `apps/web` Next.js app) surfaced a sandbox-specific limitation, not a code defect: this session's outbound network policy blocks the exact host `top-skink-67.clerk.accounts.dev` (the founder's Clerk instance's frontend API), returning an explicit "Host not in allowlist ... add this host to your network egress settings to allow access" error — both client-side (browser) and server-side (`clerkMiddleware()` running in `apps/web`'s proxy). The wiring itself is confirmed correct up to that network boundary (correctly-formed Clerk script URLs, `proxy.ts` correctly invoked). **Fix is likely on the founder's side**: add `top-skink-67.clerk.accounts.dev` to this environment's network egress allowlist (Claude Code environment settings), or test via a Vercel deployment (real infrastructure, not subject to this sandbox's allowlist) or locally/on-device. The secret key was received but deliberately not written anywhere except `apps/web/.env.local` (gitignored, confirmed via `git check-ignore`) — never committed, never echoed back in chat.

**Clerk wired into `apps/mobile` 2026-08-02:** `@clerk/expo` (v4, Clerk "Core 3" — a newer signal-based resource API than classic Clerk docs describe: `signIn.password()`/`signUp.password()` + `signUp.verifications.sendEmailCode()`/`verifyEmailCode()`, then `.finalize()` to activate the session, rather than the old `create()`/`setActive()` pattern) is installed and wired: `ClerkAuthProvider` (with the official `@clerk/expo/token-cache` for secure session persistence), `AuthGate` (gates the app behind sign-in using `useAuth()`), and a `SignInScreen` (email/password + Apple/Google via `useSSO`, since native Apple/Google hooks don't work outside a native build). **No Clerk application exists yet** — until `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` is set (see `apps/mobile/.env.example`), `ClerkAuthProvider` skips auth entirely and the app renders exactly as it did before this work, verified by re-running the same screenshot pass with zero regressions. Sign-out is wired into More but only appears once Clerk is actually active.

**Schema written and tested 2026-08-02:** `packages/database/migrations/0001_init.sql` (full schema) and `0002_rls.sql` (RLS policies) translate the Database Architecture doc into runnable, Neon-compatible SQL. Since Neon has no Supabase-style `auth.uid()`, RLS policies read a `current_user_id()` helper backed by a Postgres session variable that the API layer must set per request (via `set_config('app.current_user_id', ...)`) — documented in `packages/database/README.md`. **Both migrations were applied to a real local Postgres instance and functionally tested** (not just syntax-checked) using a non-superuser role: confirmed a partner can see a shared account but not a personal one, a stranger sees neither, a partner cannot mutate an account they don't own even when it's shared, and after Partnership disconnect both former partners keep read access while all writes freeze for both — exactly the guarantees Database Architecture §10–§11 call for. Testing also caught and fixed a real gap: the first draft had no INSERT policies on `users`/`partnerships`/`partnership_members` at all, which would have made account creation and Partnership formation impossible.

### 6.0 Backend Architecture (Phase 6, 2026-08-02)

`docs/07 Backend/Backend Architecture.md` drafted. Key decisions:

| Area | Decision | Status |
|---|---|---|
| Backend topology | **Vercel-hosted API layer (Next.js Route Handlers)** for custom logic (Plaid webhooks, billing reconciliation, AI orchestration, notifications); **Supabase stays scoped to Postgres/Auth/Storage**, not Edge Functions for business logic | Approved 2026-08-02 |
| Plaid sync | **Webhook-driven with a daily reconciliation poll as backup** | Approved 2026-08-02 |
| Background jobs | **Dedicated job/queue service — Inngest** (not Supabase `pg_cron`), given Vercel's serverless functions aren't suited to long-running/retryable workflows | Approved 2026-08-02 |
| Auth passthrough | Even Vercel-mediated requests use a user-scoped Supabase client (the caller's JWT), so RLS still governs — service-role credentials reserved only for asynchronous/system-initiated writes (webhooks, scheduled jobs) with no live user session, and even then must replicate ownership/sharing rules in code | Approved 2026-08-02 |
| Billing reconciliation | Stripe (web), Apple IAP + Google Play Billing (mobile, when it ships) all write to the same `subscriptions` table; Premium status is one computed read regardless of route | Approved 2026-08-02 |
| Client-to-Supabase pattern | Recommend clients call Supabase **directly** for simple CRUD (RLS-protected), reserving the Vercel API layer for logic needing secrets/third-party calls/cross-cutting writes | Pending — design recommendation, not yet explicitly confirmed |
| Job service specifics | Inngest picked over Trigger.dev as a reasonable default | Pending — lower-stakes, swappable |

### 6.2 API Documentation, AI Architecture, Security Architecture (baseline), Frontend Architecture (Phases 7/8/9/11, 2026-08-02)

Drafted together at founder request ("as fast as possible") — `docs/05 API/API Documentation.md`, `docs/06 AI/AI Architecture.md`, `docs/12 Security/Security Architecture.md`, `docs/08 Frontend/Frontend Architecture.md`. This completes the 5-document fast-track list from the re-sequencing decision above. Key points, in brief (full reasoning lives in each document):

- **API surface** is split between direct-Supabase access (RLS-protected CRUD) and a Vercel custom-endpoint layer for anything needing secrets/third-party calls/cross-cutting effects (Partnership invite/accept/disconnect, Wedding graduate/pause, Plaid, billing, AI). Webhook signature verification on all four inbound webhooks (Plaid/Stripe/Apple/Google) called out as non-negotiable.
- **AI Architecture:** OpenAI Responses API for both AI Coach and Purchase Advisor (model version deliberately left unpinned — an ops detail, not architecture). Context assembly uses function-calling tools that run through the same RLS-respecting path as the rest of the app — the model is structurally incapable of seeing data a user isn't permitted to see, not just instructed not to. System prompt encodes the confirmed regulatory posture (educational only, no product recommendations) and brand voice (never prescriptive, never shame). Untrusted scanned/transcribed content treated as data, never instructions (prompt-injection awareness).
- **Security Architecture (baseline only, by design)** — practical guardrails to build safely now (webhook verification, RLS discipline, secrets management, Sentry PII/financial-data scrubbing, dependency hygiene). Explicitly defers SOC 2, formal pen testing, a written incident-response plan, and adversarial prompt-injection testing to a fuller pass before public launch — not silently dropped, listed as deferred.
- **Frontend Architecture:** `packages/ui` built on React Native + `react-native-web`, so `apps/mobile` and `apps/web` share actual component code, not just business logic — a direct implementation of the "never duplicate UI" rule. TanStack Query for server state, Supabase Realtime for live shared-workspace updates, React Navigation (mobile) / Next.js App Router (web) both implementing the same 5-destination IA.

No code has been written — all four remain documentation, consistent with Rule 1, even though "fast-track to code" is the stated goal; the actual first commit of application code happens after these are reviewed.

### 6.1 Database Architecture (Phase 5, 2026-08-02)

`docs/04 Database/Database Architecture.md` drafted. Key decisions:

| Area | Decision | Status |
|---|---|---|
| Privacy enforcement | Postgres Row-Level Security is the actual privacy gate (not application code) — every personal/shared table checks `owner_id` or `partnership_id` + `is_shared` via RLS policies | Approved 2026-08-02 |
| One active Partnership | Enforced by a partial unique index on `partnership_members`, not just app logic | Approved 2026-08-02 |
| Data retention on disconnect/deletion | Shared data from a disconnected Partnership retained indefinitely as a keepsake; account deletion is request-driven, with a recommended (not yet confirmed) 30-day soft-delete grace period | Approved 2026-08-02 (retention policy); grace period is a pending recommendation |
| Balance history | Daily `account_balance_snapshots` captured from V1, not deferred | Approved 2026-08-02 |
| Audit logging | Lightweight for V1 (`created_at`/`updated_at` + soft-delete only); full audit trail deferred to Security Architecture (Phase 9) | Approved 2026-08-02 |
| Wedding family contributions | Simple ledger line (name/amount/note), no structured contributor entity, no implied account access | Approved 2026-08-02 |
| Plaid token encryption | ~~Recommend Supabase Vault (pgsodium-backed)~~ | **Superseded 2026-08-02 — see §6.4.** No longer available post-Neon pivot; replacement not yet decided |
| AI context assembly | AI service must query through the same RLS-respecting role as the app — never a service-role key that bypasses RLS — flagged as a hard rule for Phase 6/8 | Approved 2026-08-02 |

## 6.3 First Code Milestone (2026-08-02)

Founder asked to move from documentation to a visible build ("do the app"). First real code committed to `apps/mobile` and `packages/ui`, consistent with the fast-track plan — this is a **UI scaffold only**, wired to mock data, not to a real Supabase project (none has been provisioned yet).

- **Monorepo tooling:** npm workspaces (root `package.json`), not yet decided in any prior doc — a low-stakes, easily-changed choice made to get moving.
- **`apps/mobile`:** Expo SDK 57 / React Native 0.86 / React 19.2, TypeScript. React Navigation bottom tabs implementing the confirmed 5-destination IA (Home, Budget, Goals→Wedding, AI Coach, More), including the Wedding-Mode tab relabeling behavior from UX/UI Blueprint §3.2 and Design System §8.
- **`packages/ui`:** design tokens (color, typography, spacing, radius, motion) transcribed directly from Design System §2–§7, built on React Native primitives per Frontend Architecture §2 (so the same components will run in `apps/web` later via `react-native-web`). Implements the text-on-color enforcement table (Design System §2) as a `getTextColorFor()` helper, the glow-based elevation recommendation (Design System §6) on `Card`, and the ownership indicator + stacked per-partner progress bar from UX/UI Blueprint §4/§7.
- **Verified working:** ran in Expo's web mode, screenshotted via headless Chromium — all 5 tabs render correctly against mock data (a sample engaged couple, Ava & Marcus) with no console errors, and `tsc --noEmit` passes clean.
- **Not yet built:** any real backend connection (no Supabase project provisioned), Plaid/Stripe/AI integration, `apps/web`, `apps/admin`, or any of the Vercel API routes from Backend/API Architecture — this milestone is the frontend shell only.

## 7. UX Decisions

Phase 3 (UX/UI Blueprint, `docs/03 UX/UX-UI Blueprint.md`) drafted and approved-pending-sign-off 2026-08-02:

| Area | Decision | Status |
|---|---|---|
| Default appearance mode | **Dark mode primary** (Licorice background), light mode as a Settings toggle | Approved 2026-08-02 |
| Primary navigation | **5 tabs: Home, Budget, Goals, AI Coach, More** — the moodboard's 10 nav concepts consolidate into these plus nested sections (see Blueprint §3.1) | Approved 2026-08-02 |
| Partner-invite timing | Prompted early (right after solo setup), never blocking; re-prompted at natural moments if skipped, never on a nag timer | Approved 2026-08-02 |
| Premium paywall placement | **Contextual** — surfaces at the first Plaid-connection or AI-feature attempt, not upfront at signup | Approved 2026-08-02 |
| Personal vs. Shared visual model | Consistent ownership indicator (avatar glyph) on every card/row; filter/segment control rather than separate screens; sharing-status changes require explicit confirmation + activity-feed entry | Approved 2026-08-02 (Blueprint §4) |
| Onboarding sequence | Sign up → solo setup → partner invite → (if accepted) shared Home moment → Wedding Mode opt-in → contextual Premium trial offer | Approved 2026-08-02 (Blueprint §5) |

**Confirmed 2026-08-02** (previously design judgment calls, now locked before Phase 4 drafting): Wedding Mode relabels the Goals tab to "Wedding" while active, reverting to "Goals" on Graduate; Weekly Money Meeting surfaces as a Home card, not its own tab or a buried settings page.

### 7.1 Design System (Phase 4, 2026-08-02)

`docs/03 UX/Design System.md` drafted, turning Brand Guidelines + UX/UI Blueprint into concrete tokens: semantic color tokens with a hard-enforced text-on-color pairing table (Design System §2), a Bebas Neue Bold/Inter type scale (§3), a 4pt spacing grid (§4), a corner-radius scale defaulting to pill buttons (§5), and a motion timing scale with a required reduced-motion fallback per celebratory moment (§7). One recommendation is flagged as **not yet founder-confirmed**: a glow-based elevation model in place of traditional drop shadows, since flat drop shadows read poorly on the Licorice dark background (§6) — needs visual validation before being locked.

No code has been written yet — `packages/ui` stays a placeholder per Rule 1 until this document is approved; the first real code task afterward is a token file generated directly from it (Design System §9).

## 8. Open Questions

The 8 questions carried in this section as of the PRD draft were put to the founder directly on 2026-08-02 and resolved — answers are recorded in §4, §6, and §12. Reconciling those answers back into the PRD surfaced 3 narrower follow-ups, still open:

1. **Premium billing on disconnect.** The confirmed disconnect decision (§4) covers *data visibility* (frozen read-only access). It does not cover what happens to the *Premium subscription itself* when a Partnership disconnects. Working assumption, not yet confirmed: the payer keeps Premium going forward, no refund/proration.
2. **Legal review scheduling.** AI Financial Coach / Purchase Advisor guardrails are confirmed in principle (§4), but no legal review of the AI-advice posture has been scheduled. Recommend booking it before Phase 8 (AI Architecture) is finalized.
3. **Trademark/domain screening timeline.** Confirmed "Noivos" hasn't been screened yet (§9). Recommend running the screen now, in parallel with Phase 2, rather than waiting until the Marketing Website phase — a conflict found after brand work is done would waste design effort.
Items 4–7 from the previous round (icon library, Success/Warning color mapping, Wedding Mode nav placement, Money Meeting nav placement) were all confirmed by the founder on 2026-08-02 before Phase 4 drafting — see §4/§6/§7 and the Decision Log.

**New from the Supabase → Neon pivot (2026-08-02):**

8. **Plaid access-token encryption.** Supabase Vault is gone; needs a replacement (`pgcrypto` + app-managed key, or a dedicated secrets manager) before Plaid integration is built for real — not yet decided. See §6.4.
9. **Neon project provisioning.** No Neon MCP tool is available in this session, unlike Supabase's — the founder needs to create the Neon project directly (neon.tech) and provide a connection string before the app can move off mock data.
10. **Clerk application provisioning.** Similarly, no Clerk MCP tool exists — the founder needs to create a Clerk application (clerk.com), enable Apple + Google as SSO connections in the Clerk Dashboard specifically (the publishable key alone doesn't turn those on), and provide the publishable key before real sign-in works.

New from Phase 5 (Database Architecture) drafting:

1. **30-day account-deletion grace period.** Recommended in the Database Architecture doc (§12) but not one of the four questions explicitly asked before drafting — needs confirmation before the deletion-purge job is built.
2. **Plaid token encryption approach.** Supabase Vault recommended as a technical choice; full sign-off deferred to Security Architecture (Phase 9), not urgent now.

New from Phase 6 (Backend Architecture) drafting:

3. **Client-to-Supabase access pattern.** Recommended: clients call Supabase directly for simple CRUD (RLS-protected), reserving the Vercel API layer for logic needing secrets/third-party calls — not one of the three founder-confirmed questions. Flag if a single consistent API surface (everything through Vercel) is preferred instead.
4. **Inngest vs. Trigger.dev.** Inngest picked as a reasonable default for the job/queue service; low-stakes, swappable if there's a preference.

New from the Phase 7/8/9/11 fast-track drafting:

5. **React Native Web unification for `packages/ui`.** Decided outright (Frontend Architecture §2) as a direct implementation of the existing "never duplicate UI" rule rather than asked as a fresh question — flag if a fully separate web UI is actually preferred.
6. **AI model version-pinning.** Left deliberately unspecified in AI Architecture (an ops/config detail); whoever implements should pin and periodically revisit the specific OpenAI model used.
7. **Full compliance program timing.** Security Architecture is a baseline only by design — SOC 2, formal pen testing, a written incident-response plan, and adversarial AI prompt-injection testing are all explicitly deferred to a fuller pass before public launch, not scheduled yet.

New from the `apps/web` app-shell milestone (2026-08-03):

8. ~~`packages/ui` `Card` glow deprecation.~~ **Resolved 2026-08-03** — see Decision Log.

New from the founder's mid-session request to switch AI providers (2026-08-03):

9. **OpenAI vs. Ollama for AI Coach/Purchase Advisor.** §6/§6.2 confirmed OpenAI Responses API. Founder asked to use Ollama instead. Not yet decided — put directly to the founder rather than silently swapped, since it changes the backend topology (Ollama needs a self-hosted, always-on server; the current plan is Vercel serverless functions calling a hosted API), not just a model/vendor swap.

Review this list at the start of every new work session — it will keep accumulating new forks as later architecture phases (Database, Backend, AI, Security) surface decisions that need founder input.

## 9. Known Risks

- **App Store billing risk (technical/business) — mitigated, not eliminated.** Resolved 2026-08-02: launch web-only on Stripe; add Apple IAP + Google Play Billing when native mobile ships (see §6). Residual engineering risk remains: a shared entitlements service must correctly reconcile a Premium status granted via Stripe (web) with one granted via IAP/Play Billing (mobile) for the same Partnership, including trial handling, proration, and disconnect edge cases (§10 in the PRD). To be fully specified in Backend Architecture (Phase 6).
- **Regulatory risk (AI advice).** AI Purchase Advisor / Financial Coach features that answer "can we afford this," "should we refinance," "how much house can we afford" sit close to regulated financial and lending advice. Founder confirmed 2026-08-02 the AI must stay strictly educational/scenario-based (see §4) — legal review is still required before this feature ships publicly; not yet scheduled.
- **Trademark/domain risk (business).** "Noivos" is incorporated but not yet trademark- or domain-screened (confirmed 2026-08-02). Must be resolved before the Marketing Website phase and before any public launch or marketing push — a name collision found late would be costly to unwind.
- **Regulatory risk (banking data).** Plaid integration means Noivos handles sensitive financial data for two people per household. Requires bank-grade security posture (encryption at rest/in transit, least-privilege access, breach response plan) well before public launch, even though Noivos itself isn't a bank.
- **Relationship-data risk (product/legal).** Ex-partners' shared financial history must be handled with care on breakup — visibility, retention, and deletion rules need to be airtight to avoid a user's ex retaining access to their post-breakup financial life, and to avoid one partner weaponizing shared data during a breakup.
- **Two-sided activation risk (product).** The core value proposition requires both partners to opt in and stay engaged. A single-player-only outcome (one partner uses it, the other ignores invites) undermines the entire premise. Onboarding and invitation design carry outsized importance.
- **Trust/emotional-safety risk (product).** Money is a leading cause of relationship conflict. A poorly designed insight, notification, or AI response ("nudge" that reads as shaming, or an insight that surfaces one partner's spending to the other in a bad moment) could directly damage a user's real relationship. This is a uniquely high bar compared to typical fintech UX risk.

## 10. Assumptions

No open assumptions at this time — the working assumptions carried in the PRD draft were all confirmed by the founder on 2026-08-02 and have been promoted to Product/Technical Decisions (§4, §6). Future architecture phases will add new assumptions here as they come up; move each to §4/§6 once confirmed, per the process established in this round.

## 11. Future Ideas (Intentionally Postponed)

- Joint Noivos-branded account/card or any custodial money movement.
- International expansion (multi-currency, non-US banking data sources beyond Plaid's coverage).
- Financial advisor / CFP marketplace or referral integration.
- Estate planning, life insurance, or investment account management features.
- Family/multi-generational accounts beyond a two-person Partnership (e.g., involving parents contributing to a wedding or house fund) — noted as a possible V2 given "Family Contributions" appears in Wedding Mode, but full multi-party accounts are out of scope for V1.

## 12. Decision Log

| Date | Decision | Reasoning | Impact |
|---|---|---|---|
| 2026-08-02 | Kicked off Phase 1: Product Requirements Document | Founder set documentation-first methodology; PRD is first deliverable per the Documentation Roadmap | Establishes `docs/` structure, `PROJECT_MEMORY.md`, and the PRD as the source of truth for all future architecture work |
| 2026-08-02 | Adopted repository skeleton (`apps/`, `packages/`, `docs/`, `infrastructure/`) | Founder specified professional monorepo structure as a standing requirement, independent of the phased documentation roadmap | No code was written into these folders — placeholders only, pending approved architecture docs |
| 2026-08-02 | Logged founder-supplied brand moodboard as Phase-2 input, not a locked brand system | Moodboard was shared as context, not formally approved as final brand guidelines | Phase 2 (Brand Guidelines) will formally reconcile and ratify brand direction |
| 2026-08-02 | Resolved all 8 open questions from the PRD draft via direct founder Q&A: read-only-only money model, per-Partnership billing, web-first billing rollout (IAP/Play Billing added at mobile launch), US/USD/English launch scope, educational-only AI Financial Coach pending legal review, "Partnership" as the internal entity name, 14-day invite-gated free trial, and frozen-read-only disconnect visibility. Confirmed "Noivos" is incorporated but not yet trademark/domain-screened | Founder directed that remaining open questions be asked interactively via CLI rather than left as prose in chat | Promoted all 8 answers from Open Questions (§8) into Product/Technical Decisions (§4, §6); added a trademark/domain screening action item to Known Risks (§9); PRD (`docs/02 Product Requirements/PRD.md`) updated to match |
| 2026-08-02 | Approved Phase 2 Brand Statement — new color system (Electric Purple/Hot Coral/Acid Lime/Mint/Mango on Warm Ivory/Midnight), Sora/Inter typography, hand-drawn illustration style, AI/voice personality, design principles, logo and website direction | Founder supplied a complete, formal brand statement superseding the Phase 1 moodboard's specific palette and typography while keeping its underlying energy and illustration subjects | Drafted `docs/03 UX/Brand Guidelines.md`; updated §5 to record the new system as current (§5.1) while preserving the moodboard for history (§5.2); confirmed via founder Q&A that dark-mode text reuses Warm Ivory and the logo evolves the existing "N" mark rather than starting over; added icon-library choice (Lucide vs. Phosphor) as an open item |
| 2026-08-02 | Reversed the Phase 2 entry above same-day: founder confirmed the Phase 1 moodboard, not the text Brand Statement, is the default brand system | Founder clarified "the one page I sent you" meant the original moodboard image, not the Brand Statement text, when asked directly | Rewrote `docs/03 UX/Brand Guidelines.md` to v2.0: moodboard's palette (Sour Lime/Sour Punch/Electric Blue/Citrus/Grape/Sour Cloud/Licorice) and Bebas Neue Bold typography now govern; Brand Statement's non-visual content (personality, tone, AI voice, design principles, motion, photography, website direction, brand promise, north star) kept as still in force; reinterpreted "Calm before color" given the now-default palette is vivid throughout; recomputed WCAG contrast table against the moodboard's actual hex values; added Success/Warning color mapping (Sour Lime/Citrus reuse) and a "which mode is primary" open item as new recommendations pending confirmation; restructured PROJECT_MEMORY.md §5 into §5.0/5.1/5.2 to keep both rounds fully intact |
| 2026-08-02 | Drafted Phase 3 (UX/UI Blueprint): dark-mode-primary, 5-tab nav (Home/Budget/Goals/AI Coach/More), early-non-blocking partner invite, contextual Premium paywall | Founder confirmed all four via direct Q&A before drafting, since they shape onboarding and IA broadly | Wrote `docs/03 UX/UX-UI Blueprint.md`; updated §7 (UX Decisions) with the confirmed structure; added two design-judgment-call open items (Wedding Mode tab consolidation, Money Meeting placement) to §8 pending founder confirmation |
| 2026-08-02 | Confirmed the four items still open from Phases 2–3 (icon library: Lucide; Success/Warning colors: Sour Lime/Citrus reuse; Wedding Mode relabels the Goals tab; Money Meeting stays a Home card), then drafted Phase 4 (Design System) | Founder answered all four directly before Phase 4 drafting so they wouldn't get silently baked into components | Updated Brand Guidelines §20 and UX/UI Blueprint §11 to mark items resolved; wrote `docs/03 UX/Design System.md` — color/type/spacing/radius/motion tokens, text-on-color enforcement table, core component specs; flagged a glow-based elevation model (§6) as a recommendation still pending visual validation |
| 2026-08-02 | Confirmed data retention (keepsake by default, request-driven deletion), daily balance snapshots from V1, lightweight V1 audit logging (full trail deferred to Phase 9), and simple-ledger family contributions, then drafted Phase 5 (Database Architecture) | Founder answered all four directly before Phase 5 drafting since they're expensive to change once migrations exist | Wrote `docs/04 Database/Database Architecture.md` — full entity model, RLS-based privacy enforcement strategy, one-active-partnership constraint, disconnect freeze mechanics, retention/deletion policy, encryption recommendation (Supabase Vault); added a hard rule that AI/background services must never use a service-role key that bypasses RLS |
| 2026-08-02 | Re-sequenced the Documentation Roadmap: "fast-track to code" — prioritize Backend Architecture, API Documentation, AI Architecture, a Security Architecture baseline, and Frontend Architecture (the 5 remaining docs that actually gate engineering), then start building. Marketing Website, Admin Dashboard, Analytics, Testing Strategy, Launch Strategy, Business Plan, and Investor Documentation deferred, not skipped | Founder asked how much planning remained; offered a full-sequence option and a fast-track option distinguishing engineering-blocking docs from business/marketing docs that don't gate code — founder chose fast-track | Updated `docs/README.md` roadmap to reflect the new priority order and mark 7 documents as deferred-not-skipped; no change to already-approved documents |
| 2026-08-02 | Confirmed backend topology (Vercel API layer + Supabase Postgres/Auth/Storage, not Edge Functions), Plaid sync strategy (webhook + daily reconciliation poll), and job scheduling (Inngest, not pg_cron), then drafted Phase 6 (Backend Architecture) | Founder answered all three directly before drafting since they set the shape of every subsequent backend/API/AI phase | Wrote `docs/07 Backend/Backend Architecture.md` — system topology diagram, auth-passthrough rule (RLS still governs even through the API layer, service-role reserved for async/system-initiated writes only), Plaid integration flow, billing entitlement reconciliation across Stripe/IAP/Play Billing, AI service boundary (context assembly must use the same RLS-respecting path), background job list, notification dispatch |
| 2026-08-02 | Drafted the remaining fast-track documents together at founder request (API Documentation, AI Architecture, Security Architecture baseline, Frontend Architecture — Phases 7/8/9/11) | Founder said to get planning done as fast as possible; these four build directly on already-approved Backend/Database/Design decisions with no remaining founder-level forks large enough to justify pausing for Q&A | Wrote all four documents; updated §6 with a combined summary, §8 with 3 new flagged items (RN-Web unification, AI model pinning, deferred compliance program), and this log entry; completes the 5-document fast-track list — engineering can start once these are reviewed |
| 2026-08-02 | Started real implementation: `apps/mobile` (Expo/React Native) and `packages/ui` (shared design tokens/components), wired to mock data, no backend yet | Founder asked to move from documentation to a visible build, prioritizing the app over the marketing website | First code committed since the repo was scaffolded; verified via a headless-browser screenshot pass (5 tabs, no console errors) and a clean `tsc --noEmit`; full detail in §6.3 |
| 2026-08-02 | Pivoted the backend off Supabase entirely: Neon replaces Supabase Postgres, Clerk replaces Supabase Auth, Vercel Blob replaces Supabase Storage | Supabase was unworkable in the founder's environment ("unable to do Supabase"); founder directed "we can do Neon" | Updated Technical Decisions (§6) and added §6.4 documenting the full pivot; RLS design and schema (Database Architecture) carry over unchanged since they're plain Postgres features; Plaid token encryption approach is now an open question (Supabase Vault is gone, no replacement chosen yet); Neon project itself still needs to be created by the founder since no Neon MCP tool is available in this session |
| 2026-08-02 | Wrote and functionally tested the real schema (`packages/database/migrations/0001_init.sql`, `0002_rls.sql`) against a local Postgres instance | Wanted actual proof the RLS privacy model works, not just a syntax check, before it's ever pointed at a live Neon database | Applied both migrations to a scratch local database, ran adversarial-style tests as a non-superuser role (shared-vs-personal visibility, mutate-without-owning, post-disconnect freeze) — all passed; caught and fixed a real gap (missing INSERT policies on `users`/`partnerships`/`partnership_members` that would have blocked account/Partnership creation entirely) |
| 2026-08-02 | Wired Clerk auth into `apps/mobile` (`ClerkAuthProvider`, `AuthGate`, `SignInScreen` with email/password + Apple/Google via SSO) | Continuing engineering work per founder's "yes" while waiting on a real Neon connection string | Discovered the installed `@clerk/expo` v4 uses a different, newer "Core 3" signal-based resource API than classic Clerk docs describe — read the actual installed type definitions rather than guess, then implemented against the real API; built a graceful no-op fallback so the app behaves identically to before until a real Clerk publishable key is provided; re-verified with the same screenshot pass, zero regressions; added Clerk application provisioning as an open item alongside Neon (§8 item 10) |
| 2026-08-03 | Received real Clerk keys; scaffolded `apps/web` (Next.js 16 App Router) with Clerk wired in (`proxy.ts` using `clerkMiddleware()`, `<Show>`-gated header in `layout.tsx`) | Founder shared Clerk credentials plus a "Add Clerk to Next.js App Router" reference doc mid-turn, which matched already-approved Frontend/Backend Architecture (apps/web is Next.js) | Verified Next.js 16 genuinely renamed Middleware to Proxy and Clerk's Next.js SDK genuinely replaced `<SignedIn>`/`<SignedOut>` with `<Show when="...">` by reading the installed packages' real type definitions before writing code, confirming the founder-supplied reference was accurate; both `tsc --noEmit` passes clean. Testing surfaced the Clerk host is blocked by this sandbox's network egress allowlist (see §6.0 pivot note) — real end-to-end testing needs to happen outside this container |
| 2026-08-03 | Deployed `apps/web` to Vercel (`graviss-marketing/noivos`), then replaced its placeholder home page with the real 5-tab app shell (Home/Budget/Goals→Wedding/AI Coach/More), reusing `packages/ui` via `react-native-web` and the same mock-data screens already live in `apps/mobile` | Founder confirmed Clerk sign-in worked end-to-end on the real Vercel deployment (proving the sandbox's network-egress block was the only blocker, not the code), then asked to move from the placeholder to the real first screen | Vercel setup required fixing a wrong Root Directory (`./` instead of `apps/web`) and a stale per-environment "Production Overrides" config; also discovered `main` only ever had a README — `apps/web`/`apps/mobile`/`packages/*` existed solely on this feature branch — so `main` was fast-forwarded to match before Vercel (tracking `main`) could deploy anything real. For the web app itself: added `next.config.ts` Turbopack `resolveAlias` (`react-native` → `react-native-web`) since `packages/ui` imports from `react-native` directly; copied mock data and the four remaining mobile screens into `apps/web/src/screens` (no mobile-only APIs in any of them — plain `View`/`Pressable`/`TextInput`, all react-native-web-compatible); built `AppShell` as a client-side tab switcher (not React Navigation, which is mobile-only) with `onSignOut` injected by the caller rather than read via `useClerk()` directly inside `AppShell` itself — a real bug caught during testing: `useClerk()` throws with no `ClerkProvider` ancestor, which is exactly the fallback path used when Clerk env vars aren't set. Verified visually (not just `tsc`/build) via a headless-Chromium screenshot pass across all 5 tabs, and confirmed the Clerk-not-configured fallback renders the app with no dangling sign-out call. `packages/ui`'s `Card` glow effect logs a "shadow* style props are deprecated, use boxShadow" react-native-web console warning — cosmetic only, not fixed as part of this change, flagged as a small cleanup item for `packages/ui` (§8) |
| 2026-08-03 | Fixed `apps/web` rendering every heading/body variant in the browser default font instead of the brand's Bebas Neue/Inter, and resolved the `packages/ui` `Card` `shadow*` deprecation warning (§8 item 8) | Founder asked to clean up the UI/UX to match the brand image after confirming the app-shell deploy worked | Root cause: `packages/ui/src/tokens.ts`'s typography tokens reference font-family names (`BebasNeue_400Regular`, `Inter_400Regular`/`500Medium`) that only exist because `apps/mobile` registers them via `@expo-google-fonts` — `apps/web` never loaded or registered those fonts at all, so text silently fell back to the system font despite otherwise matching mobile exactly. Fixed by loading Bebas Neue + Inter via `next/font/google` in `layout.tsx` as CSS variables, and making `tokens.ts` resolve to `var(--font-bebas)`/`var(--font-inter)` on web only (`Platform.select`), leaving the native font names untouched for `apps/mobile`; also fixed the signed-out marketing placeholder's plain `<h1>`/`<p>` (outside the design-system `Text` component) and the base `body` CSS, which were still defaulting to Arial. `Card`'s glow now branches on `Platform.OS`: `boxShadow` on web, the original `shadow*` props unchanged on native. Verified via `tsc --noEmit` (both apps), a clean production `next build`, and static HTML/CSS inspection via curl (confirmed `--font-bebas`/`--font-inter` resolve to real `@font-face`-backed values and both the placeholder and the mock-data app shell reference them) — a live-browser screenshot pass was attempted but blocked by transient proxy connectivity issues in this sandbox unrelated to the code change itself |
| 2026-08-03 | Rebuilt `apps/web`'s visual/interaction layer into a real desktop dashboard — persistent sidebar + topbar above 960px (bottom tabs below it), real icons (`lucide-react-native`) replacing every emoji, a hover-interactive area chart + stat tiles + avatar-pair identity chip on Home, a circular progress ring on Budget, and a genuinely interactive AI Coach (suggestion chips, working send/reply flow, still canned — no real AI backend) | Founder shared reference screenshots from Monarch, Origin, and EveryDollar and asked the app to look and function competitively with them, explicitly calling out that the UI/UX needed real work, not just the brand-fidelity fixes from the prior entry | Scoped entirely to `apps/web` (`packages/ui`/`apps/mobile` untouched) since the ask was about the web app specifically. Followed the `dataviz` skill's procedure for every new chart (form → color → validate → marks → interaction → accessibility pass) rather than freehanding it: ran `scripts/validate_palette.js` against the existing brand categorical trio (Sour Lime/Sour Punch/Grape) used for goal contributors and found Sour Lime's raw hex fails the tool's OKLCH lightness-band check for a categorical chart mark — introduced a validated darker shade (`#638C00`) for that specific categorical context (contributor swatches, the Ava/Marcus avatar chip) while leaving the raw brand hex unchanged everywhere it's used as a single UI accent, since the check is explicitly scoped to categorical palettes, not lone accent colors. Also hit and fixed a real build break: `react-native-svg` (a `lucide-react-native` dependency) ships Metro-style `.web.js` platform variants that Turbopack doesn't know to prefer without an explicit `resolveExtensions` list, so the build was silently pulling in the native/Fabric module graph and failing on a `TurboModuleRegistry` import that doesn't exist in `react-native-web`. Verified with `tsc --noEmit`, a clean production `next build`, and a headless-Chromium pass across all 5 tabs at both 1440px and 390px widths — including confirming the trend chart's hover tooltip and the AI Coach suggestion-chip send flow actually work, not just render; caught and fixed one real UI bug in that pass (the chart tooltip's label and value were running together on one line since a plain `div` doesn't force block layout on `react-native-web` `Text` siblings the way a flex-column `View` does) |

**New open item:** the founder asked mid-session ("Let's use Ollama as our AI") to consider self-hosted Ollama instead of the OpenAI Responses API confirmed in §6/§6.2 — flagged rather than silently swapped, since Ollama's self-hosted/local-server model doesn't fit the Vercel serverless topology (Backend Architecture §1) the same way a hosted API does, and would need real infrastructure (a dedicated always-on host, not a Vercel function) to run in production. Founder then clarified the actual requirement is a **genuinely free** option, not Ollama specifically (Ollama itself isn't free once hosting is accounted for) — Google Gemini's free API tier and Groq's free tier were proposed as real $0 alternatives to OpenAI, closer to what was actually asked for. Not yet decided/confirmed; see Open Questions.
| 2026-08-03 | Wired the real signed-in person's name (via Clerk's `useUser()`) into `apps/web`'s greeting/sidebar/avatar, replacing the mock "Ava" persona for display purposes; added a Name & Birthdate setting to More that locks both fields after one save, with a "Request a change" affordance | Founder asked the app show the real account holder's name, and to add a name setting that can only be set once, with any later change requiring admin approval, plus a birthdate field | The mock financial data (contributor amounts, activity feed strings) stays tied to the illustrative "Ava & Marcus" scenario — only display-facing name usage (greeting, sidebar, avatar initial, the new setting's default) uses the real name, since rewriting the deeper mock dataset to be name-parameterized was out of scope for this ask. The Name & Birthdate setting is deliberately local-state-only (no database connected yet, confirmed with the founder before building) and the "admin approval" step is a placeholder note, not a real workflow — there's no admin system anywhere in this codebase yet. Verified: `tsc --noEmit`, a clean production build, and a headless-browser pass confirming both fields actually lock together after saving and the change-request note displays |
| 2026-08-03 | Founder created the Neon project and provided a real connection string | First real infrastructure step toward moving off mock data — Neon project provisioning was open item §8.9 | `DATABASE_URL` saved to `apps/web/.env.local` (gitignored, never committed or echoed in chat) for local work, and the founder was walked through adding it to Vercel (Production + Preview). Confirmed this sandbox cannot make raw-TCP/non-HTTPS connections at all (same class of restriction as the earlier Clerk host block, and explicitly listed as unsupported in this environment's proxy docs) — `psql` against the Neon host hung indefinitely and was killed rather than left running. Migrations (`packages/database/migrations/0001_init.sql`, `0002_rls.sql`) have **not** been applied yet; founder was pointed at Neon's own web SQL Editor to run them directly, since that's unaffected by this sandbox's restriction. Real screens are not yet wired to live data — that's a separate step once the schema exists |
| 2026-08-03 | Fixed the Partnership setting in More — "You & Marcus" / "Invite settings" / "Disconnect Partnership" had no click handler at all | Founder reported "the partner setting does not work right" | Built `PartnershipSettings`: invite flow (email input, honest "no invite-email service wired up" note rather than a fake success state) and a real disconnect flow (confirm step → flips to a genuine "not connected" state with a reconnect prompt), matching the approved frozen/read-only disconnect decision (§4) even though there's no real data to freeze yet. Local component state only, same as Name & Birthdate — no Partnership backend exists. Verified via `tsc --noEmit`, a clean production build, and a headless-browser pass through all three flows end to end; caught and fixed one real layout bug (adjacent `Text` siblings need a flex-column parent on `react-native-web`, same root cause as the trend-chart tooltip bug from the dashboard-redesign milestone) |
| 2026-08-03 | Founder ran `0001_init.sql`/`0002_rls.sql` via Neon's SQL Editor; wired the Name & Birthdate setting to real Neon persistence — the first real-data endpoint in the app (`apps/web/src/app/api/profile/route.ts`), replacing its local-state-only implementation | Founder said "continue" after confirming migrations were applied, having been told AI-provider work was deferred | Added `apps/web/src/lib/db.ts` implementing the RLS auth handoff `packages/database/README.md` documents: every authenticated query runs `set_config('app.current_user_id', ...)` and the real query on the *same connection inside one transaction* via `@neondatabase/serverless`'s `Pool` — two separate `pool.query()` calls could land on different connections and silently lose the RLS context, so this is enforced as a single `withUserContext()` helper rather than left to each call site. The one-time name/birthdate lock is now enforced in the `UPDATE`'s `WHERE display_name is null and birthdate is null` clause server-side, not just by a disabled input client-side. Added migration `0003_add_birthdate.sql` since `users.birthdate` was never in the original schema. `IdentitySettings` falls back to its prior local-state-only behavior if `/api/profile` isn't reachable (Clerk unconfigured, signed out, etc.), preserving the no-backend dev path. **Verification is partial and stated as such**: `tsc --noEmit`, a clean production build with all real env vars set, a clean 503 (no hang) confirming the Clerk-unconfigured fallback, and a full headless-browser pass of the local-fallback save/lock flow were all done from this sandbox — but this sandbox cannot make raw-TCP database connections at all (same restriction as the earlier `psql` attempt), so the actual signed-in + real-Neon-write path has **not** been exercised end-to-end; that needs to happen on the live Vercel deployment |
| 2026-08-03 | Wired the Goals screen's "All Goals" segment to real Neon data (`apps/web/src/app/api/goals/route.ts`, `.../[id]/contributions/route.ts`) — the second real-data slice | Founder said "Done. Proceed" after confirming the birthdate migration ran | Scoped to "All Goals" only — the Wedding segment's countdown/vendors/checklist stay mock (`wedding_details`/`wedding_vendors` exist in the schema but aren't wired). Chosen over Budget as the next slice specifically because Budget's "spent" side needs real transactions (no Plaid/manual-entry system exists yet), while goal contributions are manually-entered ledger lines that map directly to the existing UI with no other system required first. **Found and fixed a real RLS gap while building on this**: `goal_contributions_write` (0002_rls.sql) only checked that the inserting user named themselves as `contributor_id` — it never checked the goal itself was one they own or share, so any authenticated user who knew another user's goal UUID could have inserted a contribution against it. Migration `0004_tighten_goal_contributions_rls.sql` fixes this to mirror the existing select policy's visibility check. `GoalsScreen` falls back to the mock goals list if the backend isn't reachable, verified unchanged via a headless-browser pass. Same verification caveat as the two prior real-data entries: this sandbox can't make raw-TCP database connections, so the actual signed-in + real-write path (including confirming the new RLS fix actually blocks a stranger's goal ID) has not been exercised end-to-end from here — needs testing on the live deployment, including running migration 0004 |
| 2026-08-03 | Founder asked to hold off applying migration 0004 for now ("Keep note of this SQL. I will not be adding it now") — added a maintained migration checklist to `packages/database/README.md` tracking applied-vs-pending status explicitly, then wired Appearance (dark/light mode) to real persistence as the third real-data slice | Founder said "Proceed and keep a list of SQLs" | The migration checklist is now the single source of truth for what's actually live in the database — every new migration must be added to it as "Not yet applied" in the same commit, and flipped to Applied only once the founder confirms. Appearance required **no new migration** — `users.appearance_mode_preference` already existed in `0001_init.sql`, just wasn't connected to anything; added `POST /api/profile/appearance` (separate from the locked `/api/profile` update path, since this one has no lock) and synced `AppShell`'s `ThemeProvider` to the saved value once on mount, since it otherwise always starts at its 'dark' default and would silently override a saved 'light' preference every reload. Verified via `tsc --noEmit`, a clean production build, a clean 503 with no hang, and a headless-browser pass confirming the toggle still switches themes instantly with the backend unreachable (best-effort persistence, local mode change unaffected) — same partial-verification caveat as the other real-data slices; the live deployment still needs to confirm the saved preference actually survives a reload |
| 2026-08-03 | Wired Partnership (`apps/web/src/app/api/partnership/route.ts`, `.../invite`, `.../disconnect`) to real Neon data — the fourth real-data slice, built specifically to unblock Wedding data next | Founder said "Keep working. I'll add sql later" | `wedding_details.partnership_id` is `not null` in the schema, so nothing Wedding-related can be wired until a real `partnership_id` exists — that dependency is why Partnership was tackled now rather than after email/invite-acceptance is fully designed. Inviting creates a genuinely real Partnership + this user's membership + a pending invite row (not just local UI state); there's still no email-sending service (nothing is actually delivered) and no real accept-invite flow for a second person to join — both stated plainly in the UI. Disconnecting only updates the disconnecting user's own `partnership_members` row (`partnership_members_update`'s RLS only allows that); the former partner keeps frozen read access regardless, since `was_partnership_member()` deliberately ignores `left_at` — the partnership's own `status` flipping to `'disconnected'` is what actually gates writes for both sides. Shared `findActiveMembership()` helper was written directly into `apps/web/src/app/api/partnership/route.ts` at first and had to move to `src/lib/partnership.ts` — a Next.js `route.ts` file may only export recognized HTTP method handlers and a few config options, so an arbitrary named export there would be invalid. No new migration needed. Verified: `tsc --noEmit`, a clean production build, a clean 503 with no hang, and a headless-browser pass through disconnect → invite confirming the correct fallback-mode copy renders (distinct from the backend-real copy) when the backend isn't reachable. Same caveat as every other real-data slice: the actual signed-in + real-write path hasn't been exercised end-to-end from this sandbox |
| 2026-08-03 | Wired Wedding Mode (`apps/web/src/app/api/wedding/route.ts`, `.../vendors`, `.../checklist`, `.../checklist/[id]`) to real Neon data — the fifth real-data slice: countdown, guest estimate, vendors (add), checklist (add + toggle) | Founder said "Continue working. Do not stop" | Found and fixed a real blocker in the just-shipped Partnership work before Wedding could function at all: new Partnerships were created with `status='invited'`, but every `partnership_is_active()` RLS write check (including `wedding_details_write`) requires `'active'` — with no accept-invite flow yet to flip that, a solo Partnership could never write anything, including its own Wedding data. Changed new Partnerships to start `'active'` immediately (fast-tracked judgment call, flagged for revisit once real invite-acceptance exists — same pattern as this session's other fast-tracked calls). `GoalsScreen`'s Wedding segment now has four real states (no Partnership yet / Partnership exists but Wedding Mode not started / active with real data / mock fallback when backend unreachable), verified via mocking `/api/wedding`'s response with Playwright route interception (confirms the UI logic including live add-vendor/toggle-checklist interactions) since this sandbox still can't reach the real database for an actual signed-in test — that, the real Postgres round-trip, and the `partnership_is_active()` fix all still need verification on the live deployment. Verified from here: `tsc --noEmit`, a clean production build (12 API routes registered), a clean 503 with no hang, and the mock-fallback screenshot pass showing zero regressions |
| 2026-08-03 | Fixed `apps/web/src/screens/HomeScreen.tsx` computing Total saved, Wedding progress, the combined-savings trend chart, and the Our Wedding card from the static mock `goals` array instead of the real `/api/goals` data `GoalsScreen` already used | Caught proactively while continuing "Continue working. Do not stop" — Home and Goals would have shown different, inconsistent numbers for the same underlying goals once a real user added real goals | HomeScreen now fetches `/api/goals` on mount (same fetch-then-fallback pattern as every other real-data slice) and computes `savingsTotal`/`weddingGoal`/`weddingTotal`/`weddingTarget`/`weddingPercent` from the real data when reachable, falling back to the mock goals list otherwise; the Wedding progress tile and Our Wedding card now only render when a wedding-type goal actually exists. No new migration or API route needed — this was purely a stale-mock-data bug on the consuming screen. Verified via `tsc --noEmit`, a clean production build, a headless-browser pass with Clerk unconfigured confirming pixel-identical mock-fallback output to before the fix ($30,580 total saved, 63% wedding progress — an initial failed text assertion was a false alarm from `text-transform: uppercase` affecting Chromium's `innerText`, not a real bug), and a Playwright test mocking `/api/goals` via `page.route()` confirming the real-backend code path correctly computed $9,000 total saved and 20% wedding progress across two mock goals, isolating only the `goalType === 'wedding'` one for the Wedding stat/card. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip has been exercised from this sandbox — needs a pass on the live Vercel deployment |
| 2026-08-05 | Fixed `apps/web/src/screens/HomeScreen.tsx`'s avatar chip still showing the mock "Marcus" partner name regardless of the real Partnership state | Same class of stale-mock-data bug as the goals fix above, caught right after it — `PartnershipSettings` already pulls the real connected partner's name from `/api/partnership`, but Home never did | Added a second `useEffect` fetching `/api/partnership` on mount; uses the real `partnerName` in the avatar chip when `connected` is true, falls back to the mock name otherwise. No new migration or API route needed — `/api/partnership` GET already returned `partnerName`. Verified via `tsc --noEmit`, a clean production build, a headless-browser pass with Clerk unconfigured confirming the mock-fallback name is unchanged, and a Playwright test mocking both `/api/goals` and `/api/partnership` via `page.route()` confirming the chip correctly renders "Ava & Priya" alongside the already-verified $9,000/20% goals numbers — "Marcus" still appears in the still-mock Activity feed strings, which is expected and untouched by this fix. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip has been exercised from this sandbox — needs a pass on the live Vercel deployment |
| 2026-08-05 | Wired Budget (`apps/web/src/app/api/budget/route.ts`, `.../transactions/route.ts`) to real Neon data — the sixth real-data slice; added a "Log an expense" form per category since the mock screen was fully read-only | Founder said "Can you wire it and then we will submit the migrations later?", continuing from the "Budget/AI Coach are the remaining mock surfaces" note | No new migration needed — `accounts`/`transactions`/`categories`/`budgets`/`budget_categories` and their RLS were already in `0001_init.sql`/`0002_rls.sql`, just unused. No Plaid/bank-linking exists yet, so every expense in V1 is a manual entry against a per-user auto-provisioned "Manual Entries" account (`findOrCreateManualAccount` in the new `apps/web/src/lib/budget.ts`). First-time bootstrap seeds the same 5 default categories/planned amounts `budgetSnapshot` (the mock) used, so a first real Budget looks like what the founder was already seeing. **Fast-tracked judgment call, same posture as the Partnership `status='active'` fix**: with an active Partnership, this creates one shared `budgets` row per partnership per month (whichever partner loads Budget first) so both partners see the same numbers, but `budget_categories_write`'s existing RLS still only lets that row's owner edit it — the other partner gets a correct read-only view, not a co-editable one; flagged in `packages/database/README.md` for a real RLS pass later. Verified: `tsc --noEmit`, a clean production build (14 routes, 2 new), a headless-browser pass with Clerk unconfigured confirming pixel-identical mock-fallback output (August, $2,780 of $4,200) with the expense-logging affordance correctly hidden, and a Playwright test mocking both `/api/budget` and `/api/budget/transactions` that opened the Groceries expense form, submitted a real POST ($45 at "Trader Joes"), and confirmed the screen refetched to show $145 spent. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip — including the new shared-budget-row behavior between two real partners — has been exercised from this sandbox; needs a pass on the live Vercel deployment |
| 2026-08-05 | Wired Wedding Family Contributions (`apps/web/src/app/api/wedding/family-contributions/route.ts`, extended `GET /api/wedding`) to real Neon data — the seventh real-data slice | Founder said "Proceed except with AI and migrations. We can work on it afterwards" — Budget's slice needed no migration either, so the constraint narrowed the search to schema already covered by `0001_init.sql`/`0002_rls.sql`, and `wedding_family_contributions` fit exactly | No new migration needed — the table and its RLS were already applied, just unused. Kept as the plain free-text ledger the schema and the 2026-08-02 decision (PRD §12.8) intended: `contributor_name` is text, not a `user_id` — family members who gift money toward the wedding get logged, never real account access. `GoalsScreen`'s Wedding segment gets a new "Family Contributions" card with a log-a-contribution form (who it's from + amount), matching the add-vendor/add-checklist-item pattern already used there. Verified: `tsc --noEmit`, a clean production build (15 routes, 1 new), a headless-browser pass with Clerk unconfigured confirming the mock-fallback Wedding tab (Willow Creek Venue and friends) is unchanged and the new Family Contributions section correctly doesn't appear there, and a Playwright test mocking `/api/wedding`, `/api/wedding/family-contributions`, and `/api/goals` that rendered a seeded "Mom & Dad" / $3,000 contribution, then submitted a real POST for "Aunt Carol" / $500 through the new form and confirmed it rendered immediately from the response. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip has been exercised from this sandbox — needs a pass on the live Vercel deployment |
| 2026-08-05 | Wired HomeScreen's Upcoming Bills card (`apps/web/src/app/api/bills/route.ts`) to real Neon data — the eighth real-data slice | Continuing "Proceed except with AI and migrations" | Rather than invent a separate "bills" concept (or a new migration for one), this reads the same real `wedding_vendors.balance_due`/`balance_due_date` Wedding Mode already wired 2026-08-03 — the mock bills ("Venue deposit", "Photographer balance") were always just vendor balances under a different label. No new migration; `wedding_vendors_all`'s existing RLS already scopes the query to what the signed-in user's active Partnership can see, so no manual filtering was needed in the route itself. HomeScreen falls back to the mock `upcomingBills` list if `/api/bills` isn't reachable. Verified: `tsc --noEmit`, a clean production build (16 routes, 1 new), a headless-browser pass with Clerk unconfigured confirming the mock-fallback bills are unchanged, and a Playwright test mocking `/api/bills` (plus `/api/goals`/`/api/partnership`) that confirmed the Upcoming Bills card showed only the real mocked bill ("Real Venue Balance", $3,300, Sep 12) — read directly from the card's own text after "Venue deposit" turned out to also appear, unrelated, in the still-mock Money Meeting topics string. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip has been exercised from this sandbox — needs a pass on the live Vercel deployment |
| 2026-08-05 | Wired HomeScreen's Money Meeting card (`apps/web/src/app/api/money-meeting/route.ts`, `.../complete/route.ts`) to real Neon data — the ninth real-data slice, and the last one findable under "except AI and migrations" (Budget/Bills/Family Contributions/Money Meeting exhausted the schema already covered by 0001/0002; only AI Coach and Activity remain, and Activity's `activity_feed_events` table has no INSERT policy at all — writing to it would need a new migration, explicitly out of scope this round) | Continuing "Proceed except with AI and migrations" | No new migration needed — `money_meetings` and its RLS were already applied. Notably more permissive than Budget: `money_meetings_all` lets **any** active Partnership member update the row (not owner-scoped), so this is genuinely co-editable. The agenda is derived from real Budget (over-plan categories) and Wedding (vendor balances due within 14 days) data via plain rule-based checks in the new `apps/web/src/lib/moneyMeeting.ts` — deliberately not an AI call, computed once at meeting-creation and stored in `agenda` (jsonb) so it stays stable for the week. Added a real "Mark as done" button. Falls back to the mock agenda if there's no Partnership yet or the backend isn't reachable. Verified: `tsc --noEmit`, a clean production build (18 routes, 2 new), a headless-browser pass with Clerk unconfigured confirming the mock-fallback agenda is unchanged with no "Mark as done" button, and a Playwright test mocking `/api/money-meeting`/`.../complete` (plus `/api/goals`/`/api/partnership`/`/api/bills`) that rendered a real derived agenda item, clicked "Mark as done", confirmed the POST carried the correct meeting id, and confirmed the card flipped to "Money Meeting complete" with the button gone. Same caveat as every other real-data slice: no actual signed-in + real-Neon round trip has been exercised from this sandbox — needs a pass on the live Vercel deployment |
| 2026-08-05 | Founder applied migration `0004_tighten_goal_contributions_rls.sql` via Neon's SQL Editor after it was pasted in chat (per "Send all migrations") | Founder confirmed "success" | The `goal_contributions_write` gap flagged 2026-08-03 (any authenticated user who knew another user's goal UUID could insert a contribution against it) is now closed in the real database. `packages/database/README.md`'s migration checklist flipped to Applied — all four existing migrations are now live. |
| 2026-08-05 | Wired Activity Feed (`apps/web/src/app/api/activity/route.ts`, `apps/web/src/lib/activity.ts`) to real Neon data — the tenth real-data slice, and the first this round to need a new migration | Founder said "Next" right after confirming migration 0004 applied — with migrations back on the table, `activity_feed_events` (previously blocked, see the 2026-08-05 Money Meeting entry above) became wireable | `activity_feed_events` shipped with a SELECT-only RLS policy; `0002_rls.sql`'s comment assumed server-side-trusted-only writes (i.e. a service-role/bypass-RLS connection), which this project's hard rule forbids for live user requests. New migration `0005_add_activity_feed_insert_policy.sql` adds the missing INSERT policy — pasted in chat per the established pattern, **not yet applied** (tracked in `packages/database/README.md`'s checklist). `logActivityEvent()` is called best-effort/fire-and-forget from five other real-data routes (goal contributions, budget expenses, wedding vendor adds, wedding checklist completions, wedding family contributions) — **critical correctness point**: it runs in its own separate transaction *after* the primary write already committed, not inside the same transaction, because Postgres aborts an entire transaction after any statement error and a plain try/catch on the same connection would not have protected the primary write from a logging failure. Only shared/partnership-scoped actions are logged. HomeScreen's Activity card falls back to the mock feed if there's no Partnership or the backend is unreachable, and shows a real "No activity yet" empty state otherwise. Verified: `tsc --noEmit`, a clean production build (19 routes, 1 new), a headless-browser pass with Clerk unconfigured confirming the mock feed is unchanged, and a Playwright test mocking `/api/activity` (plus the other Home endpoints) confirming real mocked events rendered with a new `formatRelativeTime()` helper correctly converting timestamps 3 hours/2 days in the past to "3h ago"/"2d ago". **Not verified here and not yet possible**: the actual insert-then-appear round trip for any of the five instrumented routes, since migration 0005 hasn't been applied yet — needs a pass on the live deployment once it has |
| 2026-08-05 | Founder applied migration `0005_add_activity_feed_insert_policy.sql` via Neon's SQL Editor after it was pasted in chat | Founder confirmed "success" | `activity_feed_events` now has a real INSERT policy, so the `logActivityEvent()` calls already live across five write routes will actually persist instead of silently failing RLS. `packages/database/README.md`'s migration checklist flipped to Applied — **all five migrations are now live in the real database**, closing out the pending-migrations backlog that had been tracked since 2026-08-03. The insert-then-appear round trip for Activity Feed still hasn't been exercised from a real signed-in session — that, and every other real-data slice's live-database behavior, remains the one category of verification only possible on the live Vercel deployment |
| 2026-08-05 | Repainted `apps/web`'s entire visual design system toward Origin's calmer, editorial aesthetic — muted forest green/terracotta/slate blue/gold/plum on warm near-black (dark) / warm cream (light), replacing the vivid neon-lime/hot-pink/electric-blue moodboard palette on near-pure-black; Bebas Neue (all-caps condensed poster font) replaced with Fraunces (a soft editorial serif with real lowercase letterforms) for all headings | Founder said "Make it a closer look to Origin. Needs to be built to look nice. So far our UI and UX is ugly" | This is a real, founder-directed reversal of the 2026-08-02 "moodboard is the locked brand system" decision (§5.2) for *visual execution* purposes — flagged explicitly here rather than silently treated as just another polish pass, since it overrides a previously-confirmed brand direction. Executed as a token repaint, not a per-screen redesign: `packages/ui/src/tokens.ts`'s palette/typography keys kept their exact names (`sourLime`, `sourPunch`, etc.) so every screen that already referenced them by name repainted automatically — no screen-level color code had to change. Also toned down `Card`'s glow (was a wide 12px/0.35-alpha colored bloom, read as neon; now a thin tinted border + a faint neutral shadow), softened the sidebar's active-nav-item from a solid filled pill to a low-opacity tint, and reduced the card radius scale (24→18) for a more grounded, less bubbly shape. Scoped to `apps/web` only, matching the earlier Monarch/Origin/EveryDollar-reference redesign's scope — `apps/mobile`'s native font registration (`useAppFonts.ts`) was *not* updated to load Fraunces, a known flagged gap (confirmed via a clean `apps/mobile` `tsc --noEmit` that an unregistered native font name doesn't break the build, just silently falls back to the system font at runtime — unverified on an actual device/simulator, which this sandbox can't run). Verified: `tsc --noEmit` clean in both apps, a clean production build (19 API routes, unchanged from before), and a headless-browser pass across Home/Budget/Wedding/AI Coach/More at 1440px in both dark and light mode confirming the new palette/typography/Card treatment render with no regressions to any real-data wiring — screenshots sent to the founder directly for visual sign-off rather than described in prose alone |
| 2026-08-05 | Founder said "Make this compete well" — surveyed the redesigned app across desktop (1440px) and mobile (390px) widths looking for gaps vs. Monarch/Origin/EveryDollar, found and fixed two real ones | Continuing from the repaint above | **(1)** Goal-sharing contributor dot colors (Goals/Wedding "shared with" breakdowns) were still leftover neon hex literals hardcoded directly in `apps/web/src/data/mockData.ts` (`#FF2D8E`, `#8A2BE2`) and `GoalsScreen.tsx`'s `CONTRIBUTOR_COLORS` array / `AvatarStack.tsx`'s defaults (`#638C00`) — all three bypassed the token system, so the repaint above missed them entirely; a real correctness gap, not cosmetic. Fixed by referencing `packages/ui`'s palette tokens directly everywhere instead, including retiring the old `#638C00` chart-safety workaround (it existed because the *old* neon Sour Lime failed a categorical-lightness check; the *new* muted Sour Lime is almost certainly fine on its own, though `scripts/validate_palette.js` hasn't been re-run to confirm). **(2)** The topbar's Search/Notifications/Settings icons had zero `onClick` handlers — three dead buttons in every screenshot, a real "doesn't compete well" tell in a demo. Settings now switches to the More tab; Notifications now opens a real dropdown of the last 5 events from the already-wired `/api/activity` (not invented data), falling back to the mock feed the same way every other real-data slice does. Extracted the relative-time formatter (previously HomeScreen-only) into `apps/web/src/lib/formatRelativeTime.ts` so both call sites share one implementation. Search stayed inert — no cross-screen search index exists to query — but is flagged plainly via its title attribute ("Search — coming soon") and a code comment rather than left silently dead. Verified: `tsc --noEmit`, a clean production build (19 routes, unchanged), and a headless-browser pass confirming the gear switches to More, the bell's dropdown renders the mock-fallback feed correctly, and the contributor dots now render in the muted palette. The large empty whitespace below the Wedding/Goals card row (fewer cards there than on Home/Budget) was noted but not addressed this round — a possible next polish item, not a bug |
| 2026-08-05 | Added a "Spending by category" chart to Budget — a stacked-bar breakdown of the month's spend across categories, a dashboard element every competitor (Monarch/Origin/EveryDollar) leads with that was entirely missing before | Continuing "Make this compete well" | Followed the dataviz skill's procedure rather than freehanding it: form first (part-to-whole → stacked bar per `choosing-a-form.md`, explicitly not a pie/donut), color last, validated via `scripts/validate_palette.js` rather than eyeballed — confirming this project's established discipline for chart color still holds after the repaint. **Real finding**: reusing `packages/ui`'s repainted palette tokens directly for the chart FAILS the categorical validator badly (chroma floor, lightness band, CVD separation) — the muted Origin-direction palette is calm enough for UI accents but too desaturated to do identity work as a chart mark, the mirror image of the *old* neon palette's problem (too light/saturated) that originally justified the Goals contributor chart's dedicated `#638C00` shade. Derived and validated a new 5-color categorical set (`#C36E42` terracotta, `#3D71AC` blue, `#2F8F6C` teal-green, `#B08A28` gold, `#8A5FB0` plum) by iterating against this app's actual dark (`#221F17`) and light (`#FFFFFF`) surfaces until all six checks passed in both modes — order is fixed and load-bearing, re-validate before reordering. Reused the existing `StackedProgressBar` component (built for Goals) rather than a new chart primitive; shipped with a legend (name + $ per category) that doubles as the "not color alone" requirement, with the category cards immediately below serving as the accessible table-view equivalent. Verified: `tsc --noEmit`, a clean production build (19 routes, unchanged), and a headless-browser screenshot confirming correctly distinguishable segments matching each category card's real numbers — sent to the founder directly |
| 2026-08-05 | Wired the Partnership accept-invite flow (`GET /api/partnership/invite/[token]`, `POST /api/partnership/accept`, `apps/web/src/app/invite/[token]/page.tsx`) — the real second-person-joins mechanism that's been a known gap since 2026-08-03 | Founder said "Continue working for me" | The invite flow could create a real solo Partnership + a pending invite row, but nothing let a second person actually accept — `partnership_invites`' original RLS only let the *inviter* read/update their own invite. New migration `0006_add_partnership_invite_accept_flow.sql` (**not yet applied** — tracked in `packages/database/README.md`) adds two policies: any pending invite is readable by anyone (the `invite_token` UUID is the actual secret, same trust model most invite links use), and any signed-in user can transition a pending invite to accepted/declined. The new `/invite/[token]` page is a standalone landing page (no app sidebar/topbar) with sign-in/sign-up buttons that redirect back to itself post-auth, or a real Accept button once signed in. `PartnershipSettings.tsx` now shows the actual shareable link with a copy button instead of just a "no email service" note. **Known, deliberate simplification**: accepting doesn't verify the signed-in user's email matches the invite's `invitee_contact` — no real email-delivery channel exists to check against yet, so anyone with the link can accept; flagged plainly rather than assumed secure. Verified: `tsc --noEmit`, a clean production build (20 routes + 1 new page route), confirmed the Clerk-unconfigured fallback message renders without crashing, and confirmed `PartnershipSettings`' local-fallback invite flow is unchanged. **Not verified here**: the actual preview/accept UI — this sandbox's network egress block on Clerk's host (same restriction documented since 2026-08-03) prevents `ClerkProvider` from loading at all with real keys configured, so the Clerk-dependent page could only be confirmed not to crash in its Clerk-unconfigured fallback state. Needs a real pass on the live Vercel deployment, including applying migration 0006 first |
| 2026-08-05 | Founder applied migration `0006_add_partnership_invite_accept_flow.sql` via Neon's SQL Editor | Founder confirmed "Success" | `partnership_invites` now has both RLS policies an invitee needs — reading a pending invite and responding to it — so `/api/partnership/accept` can actually succeed instead of failing RLS on every attempt. `packages/database/README.md`'s migration checklist flipped to Applied — **all six migrations are now live in the real database**. The signed-in preview/accept UI still hasn't been exercised end-to-end from this sandbox (the Clerk-host network block covers that, not the migration) — that verification still needs the live Vercel deployment |
| 2026-08-05 | Brought `apps/mobile` to visual parity with the web redesign — the Origin-direction repaint had only ever touched `apps/web` | Founder said "Keep going with mobile" | `apps/mobile` shares `packages/ui` with `apps/web`, so the repainted color tokens already applied automatically — but two things didn't: the native font (Fraunces was never registered, so headings were silently falling back to the system font) and a handful of hardcoded hex literals that bypassed the token system entirely, the exact same class of bug found and fixed in `apps/web`'s `mockData.ts` on 08-05. Fixed `useAppFonts.ts` to load `@expo-google-fonts/fraunces` (`Fraunces_500Medium`/`600SemiBold` — the exact family names `packages/ui/src/tokens.ts`'s native branch already expected) and dropped the now-unused `@expo-google-fonts/bebas-neue` dependency; fixed `data/mockData.ts`'s goal-contributor dot colors (`#C6FF00`, `#FF2D8E`, `#8A2BE2`) to reference `packages/ui`'s palette directly instead. Also added the same "Spending by category" chart to `BudgetScreen.tsx` that `apps/web` got, reusing the identical validated 5-color categorical set (no new validation needed — same hex values, same surfaces) and the shared `StackedProgressBar` component. Updated `app.json`'s splash/icon background from the old `#0D0D0F` to the new `#18160F`. **Verification is limited to `tsc --noEmit` (clean)** — this sandbox has no device/simulator to actually run the Expo app, so none of this has been visually confirmed; that's a standing gap for every `apps/mobile` change this session, not new to this one |
| 2026-08-05 | Ran a systematic RLS-coverage audit (every table in `0001_init.sql` cross-referenced against every policy across `0002_rls.sql`/`0005`/`0006`) at the founder's request, plus a UI audit for the same "dead button" pattern already fixed on the topbar | Founder asked for "audit on what next" | **New RLS gaps found, all currently dormant since nothing in the app queries these tables yet — fix before building the related feature, not urgent today**: `plaid_items` (contains encrypted bank tokens) and `account_balance_snapshots` have **no RLS enabled at all**; `challenges` (the catalog table, distinct from `challenge_participations`) also has none; `attachments`, `ai_messages`, and `challenge_participations` each have a SELECT policy but **no INSERT policy**, meaning nobody could ever upload a receipt, send a real AI Coach message, or join a challenge once those features exist. Not yet fixed — logged here so they're not silently forgotten, same posture as every other tracked gap. **UI finding**: More screen's Notifications/Subscription/Support/Challenges/Milestones-shared rows are plain `<View>`s with a chevron and no `onPress` at all — the exact same "dead button" issue already fixed on the topbar, not yet fixed here. **Infra finding**: `noivos-pb6p.vercel.app` (found via the Vercel MCP tools now available in this session) has Vercel SSO Protection enabled on every URL except a custom domain, which doesn't exist yet — this blocks a real invited partner from ever reaching `/invite/[token]`, blocks mobile connectivity to the API, and blocks general public testing. Founder interrupted before any deployment-protection change was made — left as a finding for the founder to act on, not changed autonomously |
| 2026-08-05 | Applied the original brand moodboard's iconography (Home/Goals/Insights/Activity/AI Coach/Plan/Budget/Savings/Milestone/More) to `apps/web`'s primary nav and section icons | Founder resent the moodboard image and said "Use the iconography" | The 2026-08-02 decision ("Lucide for secondary/utility icons; primary nav stays bespoke per the moodboard") had never actually been matched to the moodboard's specific icon choices — `AppShell.tsx`'s nav used generic Wallet/Heart for Budget/Goals. Changed to `PieChart`/`Target` (matching the moodboard exactly); `HomeScreen.tsx`'s AI Insights card changed from `Lightbulb` to `BarChart3` and Activity changed from `Users` to the `Activity` (pulse) icon, since the moodboard's literal Activity icon shape would collide with Upcoming Bills' `CreditCard`; added a `Flag` icon (moodboard's "Plan") next to the wedding countdown in `GoalsScreen.tsx`. `apps/mobile`'s tab bar uses raw emoji, not `lucide-react-native` (not even installed there) — left unchanged rather than add an unverifiable native dependency; flagged as a follow-up. Verified: `tsc --noEmit` clean, a clean production build (21 routes, unchanged), and a headless-browser screenshot pass across Home/Budget/Wedding — sent to the founder directly, along with the live URL (`https://noivos-pb6p.vercel.app`, found via the newly-available Vercel MCP tools) |
| 2026-08-05 | Merged the two brand rounds: restored vivid, energetic accent colors close to the original moodboard, while keeping the calmer layout/typography discipline (Fraunces, grounded radius, restrained card structure) from the Origin-direction repaint | Founder: "Merge the brands together. Need the colors to be more fun" | This is the actual merge, not a reversion to either extreme — `packages/ui/src/tokens.ts`'s accent colors moved from the muted round's forest-green/terracotta/slate-blue/gold/plum back to vivid lime (`#B8F000`), the original hot pink unchanged (`#FF2D8E`), a brighter blue (`#1E7FFF`), gold-yellow (`#FFD500`), and a more energetic purple (`#9B4DFF`) than even the original. Background moved to `#141316` — a near-black with the faintest cool-neutral tone, dark enough for the vivid accents to read crisply, warmer than the original's stark `#0D0D0F` but less brown than the muted round's `#18160F`. `Card.tsx`'s glow got real presence back (a genuine `0 0 8px` soft glow at ~0.25 alpha) — more than the muted round's barely-visible tinted border, still well short of the original's wide 0.35-alpha/12px bloom. Sidebar active-nav tint opacity bumped slightly (1F→2E) for more energy against the vivid primary. **Explicitly untouched**: the dedicated categorical chart palette in `BudgetScreen.tsx` (both apps) — reusing vivid UI-accent colors directly as chart marks would fail the dataviz skill's categorical validator, same reason the muted round couldn't use them either, so that palette stays on its own separately-validated 5-color set regardless of which round the UI tokens are in. Verified: `tsc --noEmit` clean in both apps, a clean production build (21 routes, unchanged), and a headless-browser screenshot pass across Home/Budget/Wedding confirming no regressions — sent to the founder directly |
| 2026-08-05 | Wrote migration `0007_close_rls_audit_gaps.sql`, turning the previous turn's RLS audit findings into an actual migration rather than leaving them as a to-do note | Founder asked "Any missing sql?" | Closes all six gaps from the audit: `plaid_items` and `account_balance_snapshots` get RLS enabled for the first time (owner-scoped for `plaid_items`; read-only for `account_balance_snapshots` since snapshots are written by a scheduled job via service-role, not a live user request); `challenges` (the catalog table) gets a simple "any signed-in user can read" policy, no write policy (catalog is seed/admin-managed); `attachments` gets an INSERT policy scoped to `owner_id`; `ai_messages` gets an INSERT policy scoped to conversations the user themselves initiated (deliberately narrower than its SELECT policy's shared-partnership read visibility — no current product flow has a partner co-authoring someone else's AI conversation); `challenge_participations` gets an INSERT policy handling both participation shapes the schema allows (solo `user_id` or shared `partnership_id`). **Not yet applied** — pasted in chat per the established pattern, tracked in `packages/database/README.md`'s checklist |
| 2026-08-05 | Founder applied migration `0007_close_rls_audit_gaps.sql` via Neon's SQL Editor | Founder confirmed "Success" | `plaid_items`, `account_balance_snapshots`, and `challenges` now have RLS enabled for the first time; `attachments`, `ai_messages`, and `challenge_participations` all have the INSERT policies they were missing. `packages/database/README.md`'s migration checklist flipped to Applied — **all seven migrations are now live in the real database**, and the RLS audit from earlier this session is fully closed out |
| 2026-08-05 | Fixed More screen's last dead buttons (Challenges, Milestones shared, Notifications, Subscription, Support) | Founder said "Continue working" | The last item from the earlier UI audit — these were plain `<View>`s with a chevron and no `onPress` at all, same issue already fixed on the topbar. Each row is now a real `Pressable` that expands an honest "not built yet" note (specific per row — e.g. Subscription's says Noivos is free for now, Support's says to reach the team directly) instead of doing nothing, matching the app's established pattern of stating plainly what isn't real yet rather than faking it or leaving a dead tap target. Verified: `tsc --noEmit` clean, a clean production build (21 routes, unchanged), and a headless-browser pass confirming Challenges and Subscription both expand correctly. **This closes out every finding from the 2026-08-05 UI/RLS audit** — the remaining open items are the Vercel SSO/domain question (paused, founder's call) and the standing ones (AI Coach provider decision, live deployment testing, apps/mobile real-data wiring) |
| 2026-08-06 | Built a real marketing landing page for `apps/web`'s signed-out state, replacing the bare placeholder that had stood in since 2026-08-02 | Founder: "Continue to work. Build the landing page and the website of the site first" | New `apps/web/src/components/marketing/LandingPage.tsx` + `ProductPreview.tsx`, wired into `app/page.tsx` in place of `MarketingPlaceholder`. Per Brand Guidelines §17 "Website Direction" (large headlines, big product visuals, bold color moments, minimal copy, "I need this" not "here's another finance tool"): a sticky nav (logo + Sign In/Get Started), a hero ("Better money. **Together.**") with a stylized product-preview mockup, a six-feature grid, a 3-step "how it works," a final CTA band, and a minimal footer — no fake testimonials, customer counts, or press logos, since none of that exists. Every feature claim describes something that actually ships today (Budget, Goals/Wedding Mode, Partnership sharing, Money Meetings, Activity feed, the canned-reply Money Coach) — phrased carefully for AI Coach specifically, since it's still keyword-matched canned replies, not a real model, per `AICoachScreen.tsx`'s own code comment. The product-preview mockup is a stylized illustration built from `packages/ui`'s real components (`StackedProgressBar`, the same validated `BUDGET_CATEGORY_COLORS` set from `BudgetScreen.tsx`, now exported for reuse) and the same illustrative Ava/Marcus numbers already used everywhere else in the dev build (`data/mockData.ts`) — not a literal screenshot, not invented figures. **Also fixed while touching these files, since they were directly in scope**: removed `layout.tsx`'s generic floating Sign In/Up/UserButton header — it duplicated `AppShell`'s own sidebar sign-out on every authenticated page and duplicated the invite page's own sign-in/up buttons, a known gap flagged 2026-08-05 and never fixed until now; updated `globals.css`'s `:root` CSS vars and dashboard-chrome hover colors from the muted-round hex values they'd been left at to the final merge/vivid tokens; bumped `apps/mobile/app.json`'s splash/icon background from the muted round's `#18160F` to the final `#141316`; corrected `docs/03 UX/Brand Guidelines.md` §5's Color System table, which had documented the muted round this whole time even though the app has run the merged/vivid palette since later that same day — added the merge round as the current table and preserved both prior rounds for history; corrected §6's stale claim that `apps/mobile` still loads Bebas Neue (it was switched to Fraunces on 2026-08-05, just never reflected in this doc). Verified: `tsc --noEmit` clean, a clean production build (21 routes, unchanged), a curl-based check of the server-rendered signed-out HTML (Clerk *is* configured in this sandbox's `.env.local`, unlike most prior checks) confirming 200 status and all key landing-page copy present with no error digest, and a separate check with `.env.local` moved aside confirming the Clerk-unconfigured fallback (`AppShell` rendered straight through) still renders cleanly with the header removed. **Not verified here**: an actual visual/browser screenshot of the landing page — Playwright's Chromium consistently hit `ERR_TUNNEL_CONNECTION_FAILED` against `localhost` this session (worse than the usual documented transient flakiness; six-plus retries didn't clear it), so this is a server-render-only confirmation, not a pixel-level one. Founder hasn't seen a rendered screenshot of this page yet |
| 2026-08-06 | Added real SEO/social-share metadata and a brand-matched favicon/OG image — the site had none, still using Next.js's default placeholder favicon and a bare `title`/`description` | Founder: "Continue working for me" (picking up where the landing page left off, since `apps/mobile` real-data wiring and AI Coach are both blocked on decisions that aren't mine to make — see below) | New `app/icon.tsx` (64×64) and `app/apple-icon.tsx` (180×180), both generated via `next/og`'s `ImageResponse` rather than checked-in binaries — same gradient logo mark (`sourLime` → `electricBlue`) `AppShell`'s sidebar already uses. New `app/opengraph-image.tsx` (1200×630, logo + "Better money. **Together.**" + tagline) and `app/twitter-image.tsx` (re-exports the same image rather than duplicating it — some crawlers don't fall back to `og:image` for `twitter:image`). `layout.tsx`'s `metadata` export now sets `metadataBase` (the live URL), a title template, a real description, and `openGraph`/`twitter` blocks. Also tightened the landing page nav's padding/button sizing below 420px (`globals.css`'s new `.noivos-marketing-header`/`.noivos-nav-cta` rules) so Sign In/Get Started don't crowd the logo on a narrow phone — a width-reasoned defensive fix, not a pixel-verified one, same caveat as the page itself. **Explicitly did not pick up** `apps/mobile` real-data wiring or an AI Coach real backend this round: mobile's API calls would hit `noivos-pb6p.vercel.app`, which Vercel SSO Protection blocks for anything but a browser session (the 2026-08-05 audit finding, still the founder's call, not touched); and a real AI Coach backend means sending users' real financial questions to a third-party LLM provider, which the PRD's Decision Log already flagged as "pending legal review" — picking a provider unilaterally would route around that gate, not fill it. Both stay logged as open, founder-decision items rather than silently worked around. Verified: `tsc --noEmit` clean, a clean production build (25 routes, 4 new: `/icon`, `/apple-icon`, `/opengraph-image`, `/twitter-image`, all statically prerendered), and — unlike the landing page itself — an actual rendered check this time: curled all four image routes directly (bypassing the browser-navigation tunnel failure, since these are plain binary fetches), confirmed valid PNGs at the right dimensions, and visually inspected the OG image and icon directly — both render correctly on-brand |
| 2026-08-06 | Replaced `apps/mobile`'s raw-emoji tab bar icons (🏠📊🎯💍✨⋯) with the same `lucide-react-native` icons `apps/web`'s `AppShell` uses (House/PieChart/Target/Sparkles/Ellipsis) | Founder: "Continue working for me" — a concrete, unblocked item explicitly flagged as a follow-up in the 2026-08-05 icon-audit entry ("apps/mobile's tab bar uses raw emoji, not lucide-react-native, not even installed there") | Added `lucide-react-native@^1.29.0` and `react-native-svg@^15.15.5` to `apps/mobile` (matching `apps/web`'s exact `react-native-svg` version for consistency); `npx expo install` failed in this sandbox with an unrelated proxy error hitting the React Native Directory compatibility-check endpoint, so installed via plain `npm install` instead — same end state, just without Expo's own version-compatibility lookup. `RootNavigator.tsx`'s `ICONS` map is now keyed by component instead of emoji string, with both `Goals` and `Wedding` keys pointing at `Target` (the tab's underlying identity is fixed; only its display label swaps per the Wedding Mode relabel rule — mirrors `AppShell.tsx`'s equivalent map, which is keyed by a fixed tab identity separate from the label for the same reason). **Verification is a real step up from every other `apps/mobile` change this session**: beyond `tsc --noEmit` (clean), ran `npx expo export --platform android`, which actually invokes Metro to bundle the app (3077 modules, including the two new native-ish dependencies) rather than just type-checking — this is the first `apps/mobile` change all session to get an actual bundle-compiles confirmation instead of "tsc only, no simulator available." Still not visually confirmed on a real device/simulator, which this sandbox can't run |
| 2026-08-06 | Fixed the "large empty whitespace below the Wedding/Goals card row" polish item noted-but-not-addressed in the 2026-08-05 "Make this compete well" pass; added `robots.txt`/`sitemap.xml` for the marketing site | Founder: "Continue working for me" | **Whitespace fix**: `GoalsScreen.tsx`'s Wedding segment renders 4 cards (Countdown/Vendors/Checklist/Family Contributions) into a responsive 3-column desktop grid — with an odd-one-out 4th card, it landed alone in a mostly-empty row. Wrapped Family Contributions in the existing `ScreenGridWide` (already used elsewhere for section headers) so it spans full width instead — reads as "3 summary cards, then a full-width detail list," a standard dashboard pattern rather than a patch for leftover space. Scoped to `apps/web` only; `apps/mobile`'s `GoalsScreen.tsx` has no CSS grid (native single-column `View`/`ScrollView`), so the issue never existed there. **SEO completeness**: new `app/robots.ts` (allows `/`, disallows `/invite/` — the invite token is a secret, same trust model as a password-reset link, never to be crawled or cached — and `/api/`, which is all auth-gated anyway) and `app/sitemap.ts` (lists just the marketing homepage — the dashboard lives behind auth at the same URL, not a separate route). Extracted the previously-inline `SITE_URL` constant from `layout.tsx` into a new `lib/site.ts` so `robots.ts`/`sitemap.ts` share the exact same value rather than three copies of the same string drifting apart. Verified: `tsc --noEmit` clean, a clean production build (27 routes, 2 new: `/robots.txt`, `/sitemap.xml`, both statically prerendered), and curled both new routes directly, confirming correct `Disallow`/`Sitemap` lines and correct XML with the right URL/priority — same direct-fetch verification approach as the OG/icon images, since it sidesteps the still-unresolved Playwright/Chromium tunnel failure. The `GoalsScreen` whitespace fix itself could only be confirmed via server-rendered HTML (no runtime error), not a pixel-level screenshot, for the same reason |
| 2026-08-06 | Added `apps/web`'s first automated tests — the project had zero test files anywhere before this | Founder: "Let's talk about the Vercel SSO/domain decision later. Can we continue building until we need more stuff attached?" — the two remaining standing items (`apps/mobile` real-data wiring, AI Coach) both need a founder decision first, so this is genuinely unblocked, high-value work in the meantime | Added `vitest` as a dev dependency (`apps/web/vitest.config.mts`, `npm test`/`npm run test --workspace=@noivos/web` from either the app or repo root). Scoped deliberately narrow: the pure/mockable business logic in `src/lib`, not routes/components, since those mostly need a real Neon connection or Clerk session this sandbox can't provide — chasing coverage there would mean mocking away the actual thing worth testing. Three suites, 20 tests: `formatRelativeTime.test.ts` (hour/day/month thresholds, and clock-skew clamping to "just now" for a future timestamp); `moneyMeeting.test.ts` (mocks `PoolClient.query` to test `buildAgenda`'s branching/rounding/ordering logic — over-budget detection is a strict `>`, not `>=`, multiple over-budget categories, the "nothing urgent" fallback — all without needing the real SQL to run); `activity.test.ts`, covering every `event_type` `describeActivityEvent` currently handles plus the unrecognized-type fallback. **One real refactor along the way**: `describeActivityEvent` used to be a private function inside `api/activity/route.ts`, unreachable for testing — moved it into `lib/activity.ts` (which already held the write-side `logActivityEvent`) and exported it, a pure move with no behavior change, confirmed by the route's existing real-data wiring still building clean. Also fleshed out the previously one-line `README.md` (repo structure, setup, how to run each app, how to run tests) and added a root-level `npm test` convenience script. Verified: `tsc --noEmit` clean, a clean production build (27 routes, unchanged — test files aren't traced into the Next.js build), and `npm test` from both `apps/web` and the repo root, all 20 tests passing |
| 2026-08-06 | Found and fixed a real latent bug in `packages/ui`'s `StackedProgressBar` while extending test coverage to it: no guard against a zero/negative `target`, which would divide-by-zero into a `NaN%`/`Infinity%` CSS width | Founder: "Can we continue building until we need more stuff attached?" — extending the new test suite from `apps/web` to the shared `packages/ui` | Not exploitable *today* — every current caller (`BudgetScreen`'s category chart in both apps, `GoalsScreen`'s per-contributor progress) already guards with `totalCategorySpend > 0` or a goal-creation check that rejects `target <= 0` before ever rendering the bar — but it's a shared, reusable primitive with no defense of its own, so a future caller that forgets that guard would silently render broken segments instead of a clear failure. Extracted the width-percentage math into a new zero-dependency `stackedProgressBarMath.ts` (no `react`/`react-native` imports) as `computeSegmentWidths()`, added the `target <= 0` guard there, and had `StackedProgressBar.tsx` call it instead of computing widths inline. The zero-dependency split wasn't just tidiness — it's *why* this is testable at all: `react-native`'s own source uses Flow syntax that Vite/Rolldown can't parse, so importing anything from `StackedProgressBar.tsx` directly (even just for its exported helper) drags in the whole `react-native` module graph and fails to even load in Vitest; a plain `.ts` file with no such imports sidesteps that entirely. Also deleted a real piece of dead code found in the same spot: a `clampedTotal` variable computed every render and then fed into `{clampedTotal < target ? null : null}` — a ternary where both branches return `null`, doing nothing at all; removed both, no behavior change. Added `packages/ui/vitest.config.mts` (mirroring `apps/web`'s) and `packages/ui/src/stackedProgressBarMath.test.ts` (6 tests: proportional split, zero target, negative target, negative contributor amount, empty contributor list, and — locked in as intentional, not "fixed" — that over-100%-total contributions are NOT clamped, since the container's `overflow: hidden` already visually clips the excess). Verified: `tsc --noEmit` clean in both `apps/web` and `apps/mobile`, a clean `apps/web` production build (unchanged route count), `npx expo export --platform android` bundling successfully again (confirms the refactor doesn't break the native import graph either), and all 6 new tests plus the existing 20 passing |
| 2026-08-06 | Extracted `GoalsScreen.tsx`'s `daysUntil()` (drives the Wedding Mode countdown) into a testable `lib/date.ts`; found and fixed a `-0` edge case while writing its tests | Founder: "Can we continue building until we need more stuff attached?" — continuing the same test-coverage pass, one more real function found by looking for untested date/number math with real user-facing consequences | Same motivating pattern as the `StackedProgressBar` fix: `GoalsScreen.tsx` imports `View`/`Pressable`/`TextInput` from `react-native` at the top, so testing `daysUntil` in place would drag in the same unparseable Flow-syntax module graph — moved it to a zero-dependency `lib/date.ts` instead (mirrors `stackedProgressBarMath.ts`'s split). Also ran a broader diagnostic pass (`tsc --noUnusedLocals --noUnusedParameters`, not committed as a permanent tsconfig change) looking for more of the same "computed-but-never-used" class of bug across both apps — found nothing else real, just the usual harmless unused `React` import in files using the automatic JSX runtime and an intentionally-unused `request` parameter in a Route Handler, neither worth touching. **Real find while writing `date.test.ts`**: `daysUntil` can return `-0` (from `Math.ceil` of a small negative fraction, when the wedding date is today but "now" is later in the day) — not an actual rendering bug (`String(-0) === "0"`, and `-0 === 0`), but the function is typed as returning `number`, not `number | -0`, so normalized it with `|| 0` rather than leave a `-0` surprise for some future caller doing a strict-identity check. Verified: `tsc --noEmit` clean, a clean production build (27 routes, unchanged), and all 25 tests passing (5 new: null-date, the-day-itself including the explicit `-0` regression guard, the-day-before at two different times, a full month out, and gone-negative-once-passed, the last one deliberately locking in existing behavior rather than "fixing" a UX choice that isn't mine to make) |
| 2026-08-06 | Found and closed a systemic input-validation gap across every write route that accepts free text: no max-length guard anywhere, on any of it | Founder: "Keep going" — shifted from test-coverage to a security/correctness read of the write routes themselves, following the same "keep pulling on the thread" instruction | Every name/note/email-shaped column in `0001_init.sql` is a plain Postgres `text` (no built-in length limit), and Next.js Route Handlers have no request-body size limit of their own — only the platform proxy's 10MB default (`proxyClientMaxBodySize`) caps a request at all, which is still enormous for a single form field. Nothing here was exploitable in a cross-user sense (every route is already RLS-scoped, confirmed clean in the same read), but nothing stopped a client from writing megabytes of text into a single goal name, vendor name, or note. New `lib/validate.ts`: `MAX_NAME_LENGTH` (200), `MAX_NOTE_LENGTH` (2000), `MAX_EMAIL_LENGTH` (254, the real RFC 5321 SMTP-address limit) and a `tooLong()` helper — deliberately generous relative to real usage, meant to rule out abuse rather than second-guess a legitimately long note. Applied consistently across all 8 routes that accept free text: `goals` (name), `goals/[id]/contributions` (note), `budget/transactions` (merchantName), `wedding/vendors` (name, status), `wedding/family-contributions` (contributorName, note), `wedding/checklist` (title), `partnership/invite` (email), and `profile` (name) — matching each route's existing manual-`if`-check validation style rather than introducing a new validation library the rest of the codebase doesn't use. Also added `lib/validate.test.ts` (4 tests: at-the-limit passes, one-over fails, empty string passes since emptiness is a separate required-field concern, and the exact thresholds are locked in). Verified: `tsc --noEmit` clean, a clean production build (27 routes, unchanged), and all 29 tests passing |
| 2026-08-06 | Ran a real accessibility pass across both apps — found and fixed one significant keyboard-inaccessibility bug present in three places, a theme-sync bug affecting native form controls, ~20 unlabeled form fields, and closed a genuine functional gap in mobile's AI Coach screen along the way | Founder: "Let's do the accessibility pass" | **The significant find**: `<Text onPress={...}>` (not `<Pressable>`) appeared in three places — `apps/mobile/SignInScreen.tsx`'s sign-in/sign-up mode toggle, and the Dark/Light theme toggle in both apps' `MoreScreen.tsx`. Confirmed against `react-native-web`'s own source: `Text` with `onPress` only attaches a mouse `onClick` handler — no `role`, no `tabIndex`, no keyboard handling — so on the web export target these three controls were **completely unreachable by keyboard or screen reader**, not just poorly labeled. Converted all three to `Pressable` wrapping `Text` (same visual result, confirmed via screenshot). **A second real bug found along the way**: `ThemeProvider.tsx` never synced `mode` to the DOM at all — `globals.css`'s `html { color-scheme: dark }` was static and never updated when a user switched to light mode, so native browser-rendered controls (the `<input type="date">` calendar popup used in IdentitySettings/GoalsScreen, scrollbars) stayed dark-themed against a now-light page. Fixed with a web-only-guarded `useEffect` setting `document.documentElement.style.colorScheme` on mode change; confirmed via Playwright that `color-scheme` now correctly flips to `"light"` on click. **A version-specific correctness note that changed the whole pass's approach**: this repo's `react-native`/`react-native-web` versions have deprecated `accessibilityLabel`/`accessibilityState`/`accessibilityRole` in favor of direct `aria-label`/`aria-expanded`/`aria-selected`/`role` props (confirmed in both `createDOMProps`'s deprecation warnings and `react-native`'s own bundled `.d.ts` types) — per this repo's `AGENTS.md` ("heed deprecation notices"), used the modern props throughout rather than the classic RN API. Also confirmed via Playwright that RNW's `Pressable` does NOT set `role="button"` by default — only `tabIndex`; Enter-key activation works regardless, but **Space-key activation and correct screen-reader "button" announcement require an explicit `role="button"`** (verified against `PressResponder.js`'s `isValidKeyPress`/`isButtonRole` logic) — added it to every control fixed this pass, but this is a systemic gap across the many other pre-existing `Pressable`s in the app (Add/Cancel/Save buttons, etc.) that this pass did **not** attempt to fix exhaustively — flagged here as a real, scoped-out follow-up rather than silently left. **Form-field labeling**: added `aria-label` (matching each field's placeholder/purpose, disambiguated per-row where multiple identical fields render at once, e.g. "Expense amount for Groceries" vs. just "Amount") to all ~20 text inputs across `BudgetScreen`/`GoalsScreen`/`AICoachScreen` (both apps) and the native `<input>`s in `IdentitySettings`/`PartnershipSettings`, none of which had anything beyond a `placeholder` (which disappears on input and isn't a reliable accessible name). **Functional gap found and fixed**: `apps/mobile`'s `AICoachScreen.tsx` had no way to actually send a message at all — `draft` state existed but nothing ever read it, and the mic/camera "buttons" were plain `<Text>` emoji, not even `Pressable`s. Brought it to real parity with `apps/web`'s version (same canned-reply `send()`/`replyTo()` logic, real `Send`/`Mic`/`Camera` `Pressable`s with `aria-label`s, the same honestly-disabled "Share to Activity — coming soon"). Also added `aria-expanded` to `MoreScreen`'s expandable rows and `AppShell`'s Recent Activity bell (previously communicated only via a visual chevron/icon swap, nothing programmatic). **Verification was unusually strong for this session** — Playwright's Chromium finally worked again this turn (the tunnel-failure issue from the last several turns wasn't reproducible now): screenshotted the More screen before/after confirming no visual regression from the Text→Pressable wrapping, drove the Light/Dark toggle via actual `page.keyboard.press('Tab')`/`'Enter'`/`' '` from a known starting point (not just `.focus()`, which silently targets the wrong nested element) to confirm real keyboard operability end-to-end, and confirmed `role="button"` elements are present in the live DOM. Also: `tsc --noEmit` clean in both apps, `apps/web`'s full test suite (29 tests) passing, a clean production build, and `npx expo export --platform android` bundling successfully |
| 2026-08-06 | Closed the `role="button"` gap flagged as scoped-out at the end of the previous accessibility-pass entry — every remaining `Pressable` in both apps now has an explicit role | Founder: "Keep working" | Fixed the shared `packages/ui/src/Button.tsx` first for maximum leverage — it's used across 4 files, so one fix propagated everywhere it's rendered. Then swept every screen file with un-roled `Pressable`s: `HomeScreen.tsx` (1), `BudgetScreen.tsx` (3), `GoalsScreen.tsx` (14, the largest — the Wedding/All-Goals segment toggle got `role="button"` + `aria-pressed` matching the Dark/Light toggle pattern from the previous entry; the wedding-checklist item toggle got `role="checkbox"` + `aria-checked` instead, since it's genuinely a checked/unchecked state, not a momentary action), `apps/mobile/GoalsScreen.tsx` (1), and caught one `AICoachScreen.tsx` suggestion-chip Pressable the previous pass's count had actually missed (5 Pressables there, only 4 had gotten a role). Confirmed via a repo-wide grep that every `<Pressable` now pairs with a `role=` — zero remaining. Verified: `tsc --noEmit` clean in both apps, all 29 tests passing, a clean production build, `npx expo export --platform android` bundling successfully, and — Playwright kept working this session — screenshotted the Wedding and Budget tabs confirming no visual regression, plus confirmed via `document.querySelectorAll('[role="button"]')` in a live page that the segment toggle's roles are actually present in the rendered DOM, not just in the source |
| 2026-08-06 | Documented the accessibility conventions established across the last three entries in `docs/03 UX/Design System.md` §11 (new), and resolved §10's stale "focus-visible states... deferred to Frontend Architecture" open item — resolved by actually building it, not by a future spec | Founder: "Keep working" | Captured as concrete rules for future screens: Pressable-never-Text-with-onPress, every Pressable needs an explicit role, every TextInput/input needs `aria-label` absent a real `<label>`, visual-only state needs a matching ARIA attribute, theme mode must sync to `document.documentElement.style.colorScheme`. Also logged the one known-not-closed gap honestly (decorative-icon `aria-hidden`, not attempted) rather than implying the audit was exhaustive |
| 2026-08-07 | Fixed a real, currently-live bug found via a sub-agent sweep for the same "state exists but nothing consumes it" pattern that broke mobile's AI Coach: `BudgetScreen.tsx` (web) hardcoded `partnerName="Marcus"` on its `OwnershipBadge`, unconditionally, in the same render path used for both the mock fallback *and real `/api/budget` data* — meaning any real signed-in user with a real (differently-named) partner would see "Shared with Marcus" on their own Budget screen | Founder: "Keep working" — dispatched a read-only sub-agent to check `apps/mobile`'s other screens for more instances of the exact dead-state bug just fixed in `AICoachScreen.tsx`; it found no more of *that* pattern, but surfaced this hardcoded-name bug and a second dead-button gap instead | **The live bug**: fixed by dropping the hardcoded `partnerName` prop entirely on `apps/web/BudgetScreen.tsx` (matching `GoalsScreen.tsx`'s existing convention for the same component — no partner-name-fetch exists in `BudgetScreen` to plug in a real value instead, and showing a possibly-wrong name is worse than `OwnershipBadge`'s existing plain-"Shared" fallback). `apps/mobile`'s `BudgetScreen.tsx`/`GoalsScreen.tsx` had the identical hardcode, but since those screens are 100% mock data (not mixed with real fetches like web's), "Marcus" happened to be accurate — even so, referencing `currentUser.partnerName` instead of a bare string literal is the correct fix (single source of truth, and it stops being "coincidentally right" the moment mobile gets real data wiring). **The second find**: `apps/mobile`'s `MoreScreen.tsx` never got the "honest not-built-yet expandable row" treatment `apps/web`'s `MoreScreen.tsx` got on 2026-08-05 — its Partnership/Community/Account rows were still plain inert `<Text>`, the exact same dead-button pattern already fixed twice elsewhere in this app. Rewrote it to mirror web's pattern (same `Pressable` + `aria-expanded` + expand-to-reveal-note structure, now using `lucide-react-native` icons since that's installed on mobile too as of 2026-08-06). Also dropped the placeholder "Appearance" text row from the Account section — it was rendering as an inert duplicate directly below the screen's own real, working Appearance card, which read as actively wrong, not just incomplete. The Partnership section's "Invite settings"/"Disconnect Partnership" rows now honestly note they're web-only for now, rather than mobile fabricating a native port of `PartnershipSettings.tsx` (real Clipboard/native-input work, out of scope for this fix) or silently doing nothing. Verified: `tsc --noEmit` clean in both apps, `npx expo export --platform android` bundling successfully, all 29 web tests passing, and a clean `apps/web` production build |
| 2026-08-07 | Swept for more instances of the `partnerName="Marcus"` hardcode bug across the rest of the app (found none) and added test coverage for `lib/budget.ts`'s `findOrCreateManualAccount` — the one piece of dedup logic in the Budget write path that had none | Founder: "Keep working" | Grepped both apps for `"Marcus"`/`"Ava"` outside `mockData.ts`/tests — the only remaining hits were code comments describing already-fixed bugs, and confirmed every `OwnershipBadge` call site is now correct (web: no `partnerName` prop; mobile: `currentUser.partnerName`). `findOrCreateManualAccount`'s whole job is deduplication — every logged expense should land on the same one "Manual Entries" account per user, not fragment across duplicates — worth locking in since a regression here would be silent (no error, just orphaned/duplicate accounts). Two tests: reuses an existing account without a redundant insert, and creates+scopes a new one correctly when none exists yet (mocks `PoolClient.query`, same technique as `moneyMeeting.test.ts`). Also added a `DEFAULT_CATEGORIES` sanity test (every bootstrap category has a positive planned amount and non-empty name — a broken default here would ship every first-time user a visibly wrong Budget). Verified: `tsc --noEmit` clean, a clean production build (unchanged route count), and all 32 tests passing (3 new). **Assessment, stated plainly rather than continuing to manufacture rounds**: this closes out the last concrete lead from the sub-agent sweep. The remaining unblocked surface is now genuinely thin — most of what's left either needs the Vercel SSO/domain call or the AI Coach provider decision, both of which the founder has asked to hold on |
| 2026-08-07 | Found and closed five real check-then-insert race conditions via a static read of the raw SQL schema (a genuinely new angle — every prior pass this session read application code, not the DDL itself) | Founder: "Continue working" — a manual `useEffect`-dependency audit and an attempted `axe-core` automated accessibility scan both came up empty/blocked immediately before this, so this was the next distinct angle tried rather than repeating either | `money_meetings`, `budgets`, `categories`, `budget_categories`, and `accounts` (the one-manual-account-per-user case in `findOrCreateManualAccount`) all follow a "select, and if nothing's found, insert" shape with no unique constraint or `ON CONFLICT` behind it — two concurrent requests can both pass the check and both insert. Most realistically triggered by `GET /api/budget`'s default-category bootstrap, since both partners loading Budget for the first time at the same moment is a completely ordinary scenario, not an edge case — a race there would visibly duplicate every default category. `wedding_details` already got this right in `0001_init.sql` (a real `unique` column + `POST /api/wedding`'s existing `ON CONFLICT DO NOTHING`); these five never got the same treatment. New migration `0008_fix_bootstrap_race_conditions.sql` (**not yet applied**, tracked in `packages/database/README.md`) adds five unique indexes, three of them partial (`where partnership_id is null` / `where owner_id is null` / `where account_type = 'manual'`) since a plain multi-column `unique` constraint doesn't dedupe across a `null` column — exactly the personal-vs-shared split every one of these tables has. Updated all five call sites (`GET /api/money-meeting`, three spots in `GET /api/budget`, `findOrCreateManualAccount`) to use `ON CONFLICT ... DO NOTHING`, with a fallback select for the three that need the winning row's id back; the two seeding loops don't need one since a later `select` in the same request re-reads everything fresh regardless of who won. Added a test for `findOrCreateManualAccount`'s new race-loss fallback path. Verified: `tsc --noEmit` clean, a clean production build (unchanged route count), and all 33 tests passing (1 new). **Not verified and can't be from this sandbox**: the actual SQL against a real Neon connection — no raw-TCP DB access here, so the migration and every `ON CONFLICT` clause's exact match against its partial index's predicate haven't been confirmed to execute cleanly; flagged plainly in the README rather than assumed correct. Migration SQL pasted in chat per the established pattern for the founder to review and apply via Neon's SQL Editor |
| 2026-08-07 | Founder applied migration `0008_fix_bootstrap_race_conditions.sql` via Neon's SQL Editor | Founder confirmed "Applied migration 0008, continue working" | All five partial/unique indexes are now live, and — since the founder reported no error — this also confirms every `ON CONFLICT` clause across the five updated call sites matches its target index's column list and predicate exactly, something this sandbox couldn't verify on its own (no raw-TCP database access). `packages/database/README.md`'s migration checklist flipped to Applied — **all eight migrations are now live in the real database** |
| 2026-08-07 | Fixed two related-but-lower-severity issues in `partnership/invite` and `partnership/accept`: a losing race against the `one_active_partnership_per_user` unique index surfaced as a generic 500 instead of the friendly "you're already in a Partnership" 409 the checked branch already gives for the same situation | Founder: "Applied migration 0008, continue working" — kept pulling the same schema-read thread one step further, from "does a race corrupt data" (migration 0008, real bug) to "does a race at least fail nicely" (this entry, a UX/error-message issue, not a data-integrity one) | Both routes' own code comments already documented this as a known, deliberate trade-off (relying on the unique index as a backstop, accepting a generic error on the rare double-submit) — not a silent oversight, but worth closing now that this pass already established the pattern: `@neondatabase/serverless`'s thrown errors (`DatabaseError`/`NeonDbError`) expose a real `.code` field carrying the Postgres error code, confirmed via the package's own `.d.ts`. Both catch blocks now check for `code === "23505"` (`unique_violation`) first and return the same friendly, already-existing 409 message the checked-first case uses, falling through to the generic 500 for anything else. No new migration, no schema change — pure error-handling. Verified: `tsc --noEmit` clean, a clean production build (unchanged route count), and all 33 tests passing (unchanged — this is route-level error handling, which per this session's established test scope needs a real DB/Clerk session to exercise and isn't unit-testable without one) |
| 2026-08-07 | Found and fixed a severe, currently-live RLS security bug: `categories_write` in `0002_rls.sql` (applied 2026-08-03) let any authenticated user write to any other Partnership's shared categories, not just its own members | Founder: "Keep working" — pivoted from RLS *coverage* (already fully audited by migration 0007) to RLS *correctness*: reading every write/`for all` policy's actual logical shape against its siblings, rather than just checking presence/absence | Every sibling write policy (`accounts_write`, `transactions_write`, `recurring_expenses_write`, `budgets_write`, `goals_write`) `AND`s an ownership/membership check with an active-partnership check. `categories_write` alone used `OR`, and its partnership branch called only `partnership_is_active(partnership_id)` — never `was_partnership_member(partnership_id)` — so it verified a partnership was active but never that the current user actually belonged to it. Traced `apps/web/src/app/api/budget/route.ts`'s category-bootstrap code and confirmed shared categories are inserted with `owner_id = null` (unlike `budgets`, which stays owner-scoped even when shared), so the policy's `owner_id = current_user_id()` branch is never true for a shared category — meaning write access to **every** shared category on the entire platform rested on "is some Partnership active," true for any signed-in user regardless of membership. Not currently reachable through the app's own UI (no route lets a client supply an arbitrary `categoryId` for UPDATE/DELETE yet, and the one real write path — the bootstrap insert — already succeeds cleanly on first run thanks to migration 0008's unique indexes), but this is the actual database-level enforcement layer this project treats as the real privacy boundary, not application code — same posture as 0007's audit, which closed gaps in tables with zero current app usage rather than waiting for a route to reach them. New migration `0009_fix_categories_write_rls.sql` (**not yet applied**, tracked in `packages/database/README.md`) drops and recreates the policy with `AND` (matching every sibling policy's shape) and the missing `was_partnership_member()` check added to both `using` and `with check`. The change only *adds* a required condition, so it can only narrow access, never grant anything the old (buggy) policy didn't already allow — it cannot break the one legitimate write path. No application-code change needed (pure SQL/RLS); `tsc --noEmit` run clean regardless, confirming nothing else broke. Migration SQL pasted in chat per the established pattern for the founder to review and apply via Neon's SQL Editor |
| 2026-08-07 | Founder applied migration `0009_fix_categories_write_rls.sql` via Neon's SQL Editor | Founder: "success" | The `categories_write` policy is now live with `AND` and the `was_partnership_member()` check — the shared-category write hole is closed. `packages/database/README.md`'s migration checklist flipped to Applied — **all nine migrations are now live in the real database** |
| 2026-08-07 | Found a second instance of the same RLS bug class just fixed in `0009` — this time in four `WITH CHECK` clauses, not a whole policy's `OR`/`AND` shape | Founder: "Keep working" — extended the `0009` correctness read (USING vs. WITH CHECK, clause by clause) across every remaining `for all` policy in `0002_rls.sql` instead of treating `categories_write` as the only instance | `wedding_vendors_all`, `wedding_family_contributions_all`, `wedding_checklist_items_all`, and `money_meetings_all` each have a `USING` clause that correctly requires `was_partnership_member()`, but a `WITH CHECK` clause that only requires `partnership_is_active()` — never membership. Since none of these tables has any `USING` clause on `INSERT` (nothing to filter — there's no existing row yet), `WITH CHECK` alone governs every insert, meaning any authenticated user who knew (or guessed) another Partnership's `wedding_details_id`/`partnership_id` — a UUID, not secret — could insert a row falsely attributed to a Partnership they'd never joined, as long as it was active. The same gap lets a genuine member re-point an existing row at a *different* active Partnership via UPDATE, since the check never re-verifies membership on the new value either. `wedding_details_write` in the same file already had the correct symmetric shape (`using`/`with check` both requiring active + member) — proof this was an inconsistency, not a considered design choice. Not reachable through today's UI (every real write route derives the id from the writing user's own membership server-side), but the same posture as `0009` applies: fix the actual enforcement layer now rather than leave it open for whatever reaches it next. New migration `0010_fix_with_check_membership_gaps.sql` (**not yet applied**, tracked in `packages/database/README.md`) adds the missing `was_partnership_member()` check to each `WITH CHECK` clause — narrows-only, can't break any of the four real write paths, all of which already supply an id the writing user genuinely belongs to. Also re-verified every remaining `for insert`/`for update` policy across `0002`/`0004`–`0007` for the same gap class — all correctly scoped; `partnership_invites_select_pending`'s broad `status = 'pending'` visibility is confirmed as deliberate (the invite_token is the real secret), not a gap. No application-code change; `tsc --noEmit` clean. Migration SQL pasted in chat per the established pattern for the founder to review and apply via Neon's SQL Editor |
| 2026-08-07 | Founder applied migration `0010_fix_with_check_membership_gaps.sql` via Neon's SQL Editor | Founder: "success. Keep working" | All four `WITH CHECK` clauses are now live with the missing `was_partnership_member()` check — the wedding-tables/money-meetings write hole is closed. `packages/database/README.md`'s migration checklist flipped to Applied — **all ten migrations are now live in the real database** |
| 2026-08-07 | Closed the decorative-icon `aria-hidden` gap flagged as known-but-not-closed since the 2026-08-06 accessibility pass — the last open item from `docs/03 UX/Design System.md` §11 | Founder: "Keep working" — with the RLS correctness read now genuinely exhaustive (every SELECT/INSERT/UPDATE/ALL policy across all ten migrations checked for both coverage and USING/WITH CHECK correctness, no further gaps found), picked up the one concrete, previously-identified, still-open accessibility item rather than re-treading that ground a third time | Swept every file importing `lucide-react-native` in both apps (10 files: `AppShell.tsx`, `PartnershipSettings.tsx`, `LandingPage.tsx`, web's `HomeScreen`/`MoreScreen`/`AICoachScreen`/`BudgetScreen`/`GoalsScreen`, mobile's `MoreScreen`/`AICoachScreen`) and marked every genuinely decorative icon `aria-hidden={true}` — verified first via `react-native-svg`'s own web source (`prepare.js` forwards unrecognized props straight to the rendered `<svg>`, and `LucideProps` types `aria-hidden` as a real `boolean`, not a DOM string — an initial pass using the string form `"true"` failed `tsc` and was corrected). Applied to: nav/tab icons beside their own text label, icon-only buttons whose `aria-label` already supplies the accessible name, and a checklist toggle's check-mark icon *only* where it sits inside a `role="checkbox"`/`aria-checked` `Pressable` that already announces the state. **Deliberately left alone**: `GoalsScreen.tsx`'s mock-data-fallback checklist row (`!weddingBackendAvailable` branch) — a plain, non-interactive `<View>` with no `role`/`aria-checked` anywhere nearby, so its done/not-done icon is the *only* place that state is conveyed; hiding it would delete information, not de-duplicate it. Also left `apps/mobile/RootNavigator.tsx`'s `tabBarIcon` alone — React Navigation manages that control's own accessibility internally, unlike the fully hand-rolled tab bar in `AppShell.tsx`. Documented the decision criteria (decorative vs. sole-conveyor, plus the two deliberate exceptions) in Design System.md §11 so future icon additions get the same judgment call rather than being auto-exempt. Verified: `tsc --noEmit` clean in both apps, all 39 tests passing (unchanged — pure JSX-attribute change, no logic), a clean `apps/web` production build (unchanged route count), and `npx expo export --platform android` bundling successfully |
| 2026-08-07 | Found and closed a real performance gap, not a correctness one: no table in the schema has ever had a plain foreign-key lookup index | Founder: "Keep working" — after the RLS correctness read went genuinely exhaustive with no further gaps and a full sweep of every remaining unread route/lib also came back clean, pivoted from correctness to performance rather than re-reading already-clean code and calling it progress | Reading `0001_init.sql`/`0008`'s `create unique index` statements end to end (prompted by wondering what indexes already existed, going into the FK-index question) turned up that Postgres never auto-indexes foreign-key columns the way it does primary keys, and outside a handful of uniqueness constraints added for other reasons, nothing in this schema has ever had a lookup index. Worst single instance: `was_partnership_member(p_partnership_id)` — `0002_rls.sql`'s most-referenced helper, called by nearly every RLS policy in the schema — runs `where partnership_id = $1 and user_id = current_user_id()` against `partnership_members` with zero index support, meaning every row-level access check across the whole app has been a sequential table scan since `0001_init.sql` shipped. Invisible today at near-zero data volume; stops being invisible, and becomes a production incident instead of a migration, the moment real usage accumulates transaction/goal/budget history. New migration `0011_add_foreign_key_indexes.sql` (**not yet applied**, tracked in `packages/database/README.md`) adds plain btree indexes scoped to the 12 tables the app actually queries today (confirmed by grepping every table name across `apps/web/src/app`/`lib`), deliberately deferring the tables `0007` already flagged as unwired-to-any-route (`recurring_expenses`, `ai_*`, `notifications`, `challenges`/`challenge_participations`, `subscriptions`, `attachments`, `plaid_items`, `account_balance_snapshots`) and deliberately leaving `transactions.account_id` unindexed since nothing currently queries by it (noted in the migration itself, not a silent gap). Purely additive — new indexes can't change what any query returns, only how fast, so this can't introduce a correctness regression the way a policy or schema change could. Verified: `tsc --noEmit` clean in `apps/web` (no application-code change, pure SQL). Migration SQL pasted in chat per the established pattern for the founder to review and apply via Neon's SQL Editor |
| 2026-08-07 | Found and fixed a real inconsistency in `apps/mobile`'s `GoalsScreen.tsx`: its Wedding-mode checklist rendered done/not-done state with raw `✅`/`⬜️` emoji instead of the `CircleCheck`/`Circle` lucide icons every other checked/unchecked indicator in the app uses (web's `GoalsScreen`, mobile's own `MoreScreen`) | Founder: "Keep working" — after the FK-index migration, checked mobile's `TextInput`s for the same `aria-label` coverage the web accessibility pass established (all three already covered — a clean negative result), then widened the check to a repo-wide emoji grep, since this file had never been touched by the earlier `lucide-react-native` sweep (it didn't import that package at all until now) | This row is plain mock display — no `Pressable`, no `role="checkbox"` (mobile's Wedding Mode has no real backend the way web's does), so the icon is the *only* place done/not-done state is conveyed, same shape as web's own mock-fallback checklist row. Emoji are the wrong tool for that: screen readers announce them by whatever Unicode name happens to map to the character ("check mark button", "white large square button") rather than a controlled accessible name, and they render inconsistently across platforms/OS emoji-font versions — where a real vector icon is both consistent and controllable. Swapped for the same `CircleCheck`/`Circle` pair already used everywhere else in the app for this exact purpose, left without `aria-hidden` for the same sole-conveyor reasoning as web's equivalent row. Also grepped both apps for any other emoji used as UI content (not just this one): `mockData.ts`'s two `🎉` occurrences and mobile `HomeScreen.tsx`'s `💡` insight-line prefix are all purely decorative flourishes inside a full text sentence that already carries the actual meaning — left alone, not a bug. Verified: `tsc --noEmit` clean in `apps/mobile`, `npx expo export --platform android` bundling successfully, and all 33 `apps/web` tests still passing (unrelated to this change, confirms nothing else broke) |
| 2026-08-07 | Ran `npm audit` for the first time this session — a genuinely fresh angle (dependency hygiene, not code correctness) — and fixed the one high-severity finding that had a safe, non-major upgrade path: `next` 16.2.12 → 16.3.0, which also resolves bundled `postcss` (XSS + two path-traversal CVEs in its sourceMappingURL handling) and `sharp` (four libvips CVEs) advisories, both real production-path dependencies (CSS processing and `next/image` optimization) | Founder: "Keep working" | 36 vulnerabilities found total; deliberately left the other 33 alone rather than blindly running `npm audit fix --force` — every one of them requires a semver-major bump (`expo` 53.0.27, `@clerk/expo` 3.7.8, `react-native` 0.72.17) that `AGENTS.md` itself already warns is high-risk ("Expo HAS CHANGED... read exact versioned docs before writing any code"), and nearly all trace back to `@clerk/clerk-js`'s optional Solana wallet-connect support (`@solana/wallet-adapter-*`, `@solana-mobile/*`) — a code path this app never invokes (no crypto wallet integration exists or is planned), so the actual exploitable surface for those 33 is effectively nil versus the real risk of a blind major-version mobile toolchain upgrade breaking the app. `next`/`eslint-config-next` bumped together (kept in lockstep, standard practice) to the exact version `npm audit`'s `fixAvailable` reported as non-major. Running `eslint .` directly (not done earlier this session, which relied on `tsc`/tests/build) surfaced one more real, pre-existing, unrelated finding while verifying the upgrade: `apps/web/src/app/invite/[token]/page.tsx`'s "Go to Noivos" link used a raw `<a href="/">` instead of `next/link`'s `<Link>`, losing Next.js's client-side navigation. Fixed alongside the dependency bump since it was surfaced by the same verification pass. Verified: `tsc --noEmit` clean, `eslint .` clean (zero errors, was one before), all 39 tests passing, a clean `apps/web` production build (unchanged route count), and `npx expo export --platform android` still bundling successfully (mobile untouched by this change, confirmed regardless) |
| 2026-08-08 | Checked `apps/mobile`/`packages/ui` for the same eslint coverage `apps/web` has (neither has an eslint config or lint script at all — a real gap, but standing up rule sets for two more packages is a bigger infrastructure decision than a bug-hunt round should make unilaterally, so left as a noted gap rather than acted on) and instead grepped both apps for `any`-typed escape hatches, a class of type-safety gap this session hadn't checked yet | Founder: "Start working again" | Found two: `AppShell.tsx`'s `Record<Tab, ComponentType<any>>` already carries an explicit `eslint-disable-next-line` comment right above it — a deliberate, acknowledged trade-off (the five screen components genuinely have incompatible prop signatures), left alone. `apps/mobile/SignInScreen.tsx`'s two `catch (err: any)` blocks were unmarked and unconsidered — the one place in the app that has to catch Clerk SDK errors directly (web uses Clerk's own pre-built `<SignInButton>`/`<SignUpButton>`, never a custom form) — reading through to `err?.errors?.[0]?.longMessage` blind. `@clerk/expo` re-exports Clerk's own `isClerkAPIResponseError(error: unknown): error is ClerkAPIResponseError` type guard (confirmed via the installed package's `.d.ts` chain) built for exactly this shape; swapped both catches to `err: unknown` narrowed through a small `clerkErrorMessage()` helper, matching the `instanceof`-based error narrowing convention this session already established for the API routes' own catch blocks (the `err.code === "23505"` pattern). Functionally identical behavior (the old code's optional chaining already degraded safely on a shape mismatch) — this is a type-safety correction, not a bug fix. Verified: `tsc --noEmit` clean in `apps/mobile`, `npx expo export --platform android` bundling successfully, and all 39 `apps/web` tests still passing (unrelated to this change) |
| 2026-08-08 | Wired two real HomeScreen cards that had stayed mock since the original build: the Budget snapshot (StatTile + summary Card) now pulls from real `/api/budget`, and "AI Insights" now computes real "basic insights" (PRD §13's free-tier term, distinct from Premium's model-driven "advanced AI Insights") from that same budget data plus real goal totals | Founder: "Continue building" — a genuine pivot from this session's long correctness/security/performance/hygiene audit run back to shipping product surface, since the remaining unblocked audit leads had run dry | Both cards were flagged in this file's own header comment as staying mock ("no shared-transactions feed or AI backend exists yet") — stale by the time of this pass, since `/api/budget/transactions` had gone real back on 2026-08-05/06. Checked the PRD before touching anything AI-labeled: §13's tier table lists "basic insights" in the free tier and only "**advanced** AI Insights (predictive, trend-based)" behind Premium, and §12.11's own example list ("budget category drift," "opportunity") is plainly rule-based-computable from a single data snapshot — this is the same category of feature as Money Meeting's agenda (`lib/moneyMeeting.ts`'s `buildAgenda()`, real since 2026-08-05, explicitly "not an AI call"), not the conversational AI Purchase Advisor/Financial Coach that's genuinely gated on the provider decision. New `apps/web/src/lib/insights.ts` + `insights.test.ts` (10 tests): a pure `buildInsights(categories, goals)` function flagging (a) any category where `spent > planned` ("`{name}` is running $`{over}` over plan this month — worth a look?") and (b) any goal at or near (≥95%) full funding — deliberately *not* attempting PRD §12.11's trend-dependent examples ("rising dining spend," "savings streak," "large/unusual purchase") since there's no historical time-series data to support them honestly yet (no daily balance snapshots, no recurring_expenses wiring) — a defensible v1 subset over an overreaching one. `HomeScreen.tsx` now fetches `/api/budget` with the same independent-fallback pattern as `apiBills`/`apiMeeting`/`apiActivity` (not gated on the shared `backendAvailable` flag the Goals fetch uses), and calls `buildInsights()` against real-or-mock data uniformly — replacing the old static `insights` mock array entirely (removed from `mockData.ts`) rather than only wiring the real-backend branch, so the mock-fallback path exercises the same real logic instead of separately-maintained placeholder text. Also fixed a pre-existing, unguarded divide-by-zero in the Budget progress bar's width calculation while touching that exact line (mirrors `stackedProgressBarMath.ts`'s `target <= 0` guard from 2026-08-06). Verified: `tsc --noEmit` clean, `eslint .` clean, all 43 tests passing (10 new), a clean production build (unchanged route count). **Not verified**: the actual rendered HomeScreen with a real signed-in session — this sandbox's long-standing Clerk network-egress block (documented since 2026-08-03) means there's still no way to reach the authenticated app from here; a fresh Playwright attempt against the mock-fallback marketing page also hit the same `ERR_TUNNEL_CONNECTION_FAILED` flakiness noted in the blocked axe-core attempt earlier this session. Flagged honestly rather than assumed working — worth a manual check on the live Vercel deployment |
| 2026-08-08 | Found and fixed a real, currently-live bug affecting every signed-in user on every page: `AppShell.tsx`'s nav tab (and `GoalsScreen.tsx`'s own H1/segment-default) has said "Wedding" instead of "Goals" for every real user, regardless of whether their actual Partnership ever started Wedding Mode | Founder: "Continue" — after wiring HomeScreen's last two mock cards, kept pulling the same "is this mock flag actually gating real UI" thread one level up, into the always-visible chrome around every screen rather than just card content | Both components read `weddingDetails.active` — a static mock export hardcoded `true` in `mockData.ts` — to decide whether the top nav tab reads "Wedding" vs "Goals" (`AppShell.tsx`) and whether `GoalsScreen.tsx`'s own H1/segment-toggle/default-view treats Wedding Mode as on. Since the mock flag is unconditionally `true`, this was never actually gated on anything real — every signed-in user, with or without Wedding Mode ever started, has seen "Wedding" everywhere since this UI first shipped. `GoalsScreen.tsx` itself already fetches `/api/wedding` and knows the real answer (`hasPartnership && weddingDetails !== null`, matching how its own inner content already branches deeper down) but never used that value for its own top-level framing — the mock override sat one level above where the real data actually lived. Fixed both: `GoalsScreen.tsx` now derives a real `weddingActive` from its existing fetch (defaulting its initial `segment` state to the safe "goals" before the fetch resolves, then flipping to "wedding" once it genuinely knows — can't know synchronously on first render); `AppShell.tsx` gained its own small `/api/wedding` fetch (matching this app's established "each component fetches its own data independently" convention, same posture as its existing `/api/profile` appearance-mode fetch, rather than lifting state or prop-drilling from GoalsScreen), also defaulting to the safe "Goals" label while loading. Both fall back to the mock's own `.active` flag *only* on genuine fetch failure — deliberately kept in sync so the tab label and the screen it leads to can't contradict each other in dev/preview mode with no backend reachable at all (a real risk once both components independently decide this, not present when only one hardcoded source existed). Verified: `tsc --noEmit` clean in both apps, `eslint .` clean, all 43 tests passing (unchanged — this is UI-state wiring, not new business logic), and a clean production build (unchanged route count). **Not verified**: same sandbox limitation as every other HomeScreen/GoalsScreen change this session — no real signed-in session reachable here to visually confirm the tab now genuinely reads "Goals" for a non-Wedding-Mode account |
| 2026-08-08 | Founder confirmed via `/ask`: keep Vercel SSO protection as-is (no domain change), leave AI Coach's real backend fully alone (still gated on legal review, nothing to prep in the meantime), and keep sweeping for mock-to-real gaps rather than start new PRD scaffolding | Founder, structured CLI questions after "Can you continue to work on this? Ask questions." | Recorded so this isn't re-litigated next session: Vercel deployment protection (`ssoProtection.enabled: true`, `all_except_custom_domains`) was checked read-only via the Vercel MCP tool to inform the question, not changed. |
| 2026-08-08 | Found and fixed two more instances of the "specific fake partner name shown to a real user regardless of their actual connection status" bug class (same root cause as BudgetScreen's earlier `partnerName="Marcus"` fix), surfaced by grepping every remaining `mockData.ts` export for ungated usage across the app | Founder: confirmed "keep sweeping for mock-to-real gaps" | `AICoachScreen.tsx`'s "SHARE WITH {name}" caption showed `currentUser.partnerName` ("MARCUS") unconditionally — the screen has zero real fetches anywhere else (still fully canned per its own posture), so adding a whole new `/api/partnership` fetch just to personalize one caption would be disproportionate; used a neutral, never-wrong "YOUR PARTNER" instead, matching `OwnershipBadge.tsx`'s own established precedent of omitting a name entirely rather than fabricating one when it isn't actually known. `HomeScreen.tsx`'s `AvatarStack` (`names={[displayName, partnerName || currentUser.partnerName]}`) had the same bug in a subtler form: `partnerName` stays `null` both while loading *and* when `/api/partnership` genuinely confirms `connected: false` (an "Unpartnered" account — PRD §10.3's own words: "a valid, supported, permanent state," not a limbo to paper over), so a real, genuinely-solo user would see a fabricated "& Marcus" couple-chip next to their own name. Confirmed this was a real inconsistency, not an ambiguous design choice, by checking `PartnershipSettings.tsx`'s own partner-name fetch on the very same page — it already correctly distinguishes "connected: false" (`setPartnerName(data.partnerName ?? null)`) from "unreachable" (mock only in `.catch()`); `HomeScreen.tsx`'s fetch never made that distinction. Fixed with a new `partnershipChecked` boolean (true once `/api/partnership` has genuinely answered, regardless of the answer) so the `AvatarStack` renders in every ambiguous case (loading, unreachable — same illustrative-mock posture as every other card) but is cleanly omitted only once solo status is certain, without inventing new placeholder copy/visual treatment for a component whose two-avatar design wasn't built for a one-person state. Verified: `tsc --noEmit` clean in both apps, `eslint .` clean, all 43 tests passing (unchanged — pure UI-state wiring), a clean production build (unchanged route count). **Not verified**: the actual rendered screens with a real signed-in session, same sandbox limitation as every other identity-wiring change this session |
| 2026-08-08 | Found and removed a hardcoded, unconditional false numeric claim: HomeScreen's "Total saved" StatTile said "▲ 4.2% this month" for every user, with zero connection to any data, real or mock | Founder: "Continue" — kept sweeping for the same "fabricated fact shown as real" bug class, this time checking for hardcoded claims beyond just partner names (`deltaLabel="..."` string literals, percentages, streaks) rather than assuming the class was fully closed | Unlike every other fix in this class this session, there's no real data to wire this *to* — no historical balance data exists at all yet (`account_balance_snapshots` has zero current app usage per `0007`'s audit, so there's no real "% this month" to compute). Deriving a number from the tile's own `useSavingsTrend()` sparkline shape wouldn't have been an improvement — that shape is itself synthetic (a fixed growth curve `[0.78...1.0]` scaled to today's real total, already disclosed in its own comment), so any percentage extracted from it would just be a *different* fabricated number wearing a more convincing disguise, not a real one. Removed the `deltaLabel`/`deltaDirection`/`deltaIsGood` props entirely (all optional on `StatTile`, so this cleanly drops just the delta line) rather than inventing a replacement claim. Left the sparkline itself alone — a meaningfully lower-severity fabrication (explicitly "decorative," no axis/hover/stated value per its own component comment) than a specific quotable false percentage, and actually fixing it needs new daily-balance-snapshot infrastructure, a real feature build, not a wiring fix — flagged here rather than half-fixed with more improvised math. Swept the rest of the app for the same pattern (hardcoded `deltaLabel` string literals, stray percentages/streak claims in JSX) — the only other hits were inside `AICoachScreen.tsx`'s canned-reply text, which is already fully disclosed as non-real and explicitly out of scope per the founder's own "leave AI Coach alone" answer earlier today, not a silent gap. Verified: `tsc --noEmit` clean, `eslint .` clean, all 43 tests passing (unchanged — pure prop removal), a clean production build (unchanged route count) |
| 2026-08-08 | Found and fixed the most severe instance yet of the "fake partner data shown to a genuinely solo real user" bug class: both `HomeScreen.tsx`'s Activity card and `AppShell.tsx`'s Recent Activity bell dropdown showed fully fabricated activity *events* (not just a name) — things like "Marcus joined your Partnership" — to any real user with no partnership at all | Founder: "Continue" — checked `AppShell.tsx`'s bell dropdown for the same "genuinely-empty vs. unreachable" distinction just fixed for the AvatarStack/partner-name bugs, on the theory that the same root pattern (an `else`/fallthrough branch conflating "confirmed no partner" with "couldn't reach the backend") could recur anywhere else `/api/activity`'s `hasPartnership: false` response was handled | Both call sites had a real, successful fetch response confirming `hasPartnership: false` (PRD §10.3's "Unpartnered... a valid, supported, permanent state," the *same* real answer already fixed for `partnershipChecked` two entries ago) but treated it identically to a network failure — `HomeScreen.tsx`'s effect did a bare `return` without ever setting `apiActivity`, leaving it `null` forever and falling through to the mock `activityFeed`; `AppShell.tsx`'s effect explicitly called `setActivityEvents(mockActivityFeed)` in that exact branch. Both components already had a correct, unused "No activity yet" empty state sitting right there in their own render logic (`apiActivity && apiActivity.length === 0` / `activityEvents?.length === 0`) — the fix was purely routing the confirmed-solo case to `[]` instead of the mock, no new UI needed. Verified: `tsc --noEmit` clean, `eslint .` clean, all 43 tests passing (unchanged — pure state-branch fix), a clean production build (unchanged route count) |
| 2026-08-08 | Found and fixed a third instance of the same `hasPartnership: false` bug — grepped every remaining call site of that field across the app after fixing Activity, and found `HomeScreen.tsx`'s Money Meeting fetch had it too: a genuinely solo real user was shown the mock Money Meeting card, with fabricated agenda topics ("Wedding budget is $340 over on Dining," "Emergency Fund is ahead of schedule") about a partnership that doesn't exist | Founder: "Continue" — after finding the pattern recurred once (Activity, in two places), grepped for every other `hasPartnership` usage across the app rather than assuming those were the only two instances | Unlike Activity, Money Meetings have no honest "empty" state to fall back to — `money_meetings` requires a non-null `partnership_id` (no solo concept exists in the schema at all), so there's nothing meaningful to show a genuinely-solo user instead of the real thing. Rather than invent new "invite your partner" copy for yet another card (risk of cluttering Home with a message GoalsScreen's own Wedding Mode prompt already covers), omitted the card entirely once confirmed solo — same minimal-footprint approach as the `AvatarStack` fix. New `meetingHasPartnership: boolean | null` state (`null` = still loading or unreachable, falls back to the mock card exactly as before; `false` = a real, confirmed answer, hides the card; `true` = real meeting data shown). Verified: `tsc --noEmit` clean in both apps, `eslint .` clean, all 43 tests passing (unchanged — pure state/rendering fix), a clean production build (unchanged route count) |

---

*Do not delete history from this file. Append new sections and log entries as decisions are made. If this file conflicts with a new document or instruction, stop and ask the founder before proceeding.*
